---
phase: 26-release-v1.5.0
plan: 01
type: release-prep
wave: 1
depends_on: []
files_modified:
  - maestro-menu-editor.php
  - readme.txt
  - package.json
  - package-lock.json
autonomous: false
requirements: [REL-11]
must_haves:
  truths:
    - "Every version string reads 1.5.0, bumped via bin/prep-release.sh rather than by hand."
    - "The changelog is derived from the `v1.4.1..main` DIFF, not from the phase list."
    - "The Upgrade Notice for 1.5.0 is under Plugin Check's 300-character limit."
    - "The multisite super-admin exemption is stated as a known limitation, not omitted."
  artifacts:
    - path: "readme.txt"
      provides: "1.5.0 changelog + Upgrade Notice + Stable tag"
      contains: "1.5.0"
---

<objective>
Prepare the 1.5.0 release content: version strings, changelog, Upgrade Notice.

Purpose: v1.4.0's gate caught a changelog OVERCLAIM at gate 8 — the changelog
said shared-slug collisions were fixed in both directions when only the submenu
direction isolates. That was caught because someone diffed the actual commits.
This plan does that first rather than last.
Output: a release-ready readme.txt and bumped version strings, reviewed against
the real diff.

**`autonomous: false`** — the changelog is user-facing copy about a security-
adjacent feature; a human should read it before it ships.
</objective>

<context>
@.planning/STATE.md
@readme.txt
@bin/prep-release.sh

<interfaces>
<!-- Contracts the executor builds on. Use directly — no exploration needed. -->

`bin/prep-release.sh <VERSION>` bumps, in one pass:
  - maestro-menu-editor.php  (`* Version:` header + MAESTRO_VERSION constant)
  - readme.txt               (`Stable tag:`)
  - package.json + lockfile
Takes a bare semver (`1.5.0`, no leading `v`). Do NOT hand-edit these.

**The actual user-facing diff, `v1.4.1..main`** (planning-doc commits excluded):

| Commit | User-facing change |
|---|---|
| `2a18acc` `54c982b` `551db33` | ROLE-02 per-user hiding — the headline |
| `fb06067` `6c201cd` | Its guardrail + e2e (no user-facing behaviour) |
| `a1769f9` `23fa96c` | Codex fixes: self-target stays undoable; `list_users` gating; 50-user cap; server-side gate |
| `c0f459b` | Pure refactor, no behaviour |
| `0fe9ba7` | js-yaml dev-dependency bump (dev-only; NOT user-facing, do not claim it) |
| `f80b210` | Perf docs only |
| `37719b3` | Test-harness stability only |

So the changelog has exactly ONE user-facing story: per-user cosmetic hiding.
Resist padding it with the rest.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bump version strings</name>
  <files>maestro-menu-editor.php, readme.txt, package.json, package-lock.json</files>
  <behavior>
    - All four files read 1.5.0 / Stable tag 1.5.0.
    - No file is hand-edited; the script is the single source of truth.
  </behavior>
  <action>
    Run `bin/prep-release.sh 1.5.0` and inspect the resulting diff. If the script
    misses a location, FIX THE SCRIPT rather than patching the file by hand — a
    hand-patched string is one that silently drifts at the next release.
  </action>
  <verify>
    <automated>grep -n "1\.5\.0" maestro-menu-editor.php readme.txt package.json</automated>
  </verify>
  <done>All version strings at 1.5.0 via the script; diff reviewed.</done>
</task>

<task type="auto">
  <name>Task 2: Changelog + Upgrade Notice, written from the diff</name>
  <files>readme.txt</files>
  <behavior>
    - A `= 1.5.0 =` changelog section describing per-user hiding in user terms.
    - It states the cosmetic-only guarantee — hidden pages still load by URL.
    - It states the multisite super-admin exemption as a known limitation.
    - It does NOT claim the cloned-role/profiles half exists.
    - It does NOT list dev-only changes (js-yaml, test harness, perf docs) as
      user-facing.
    - An `= 1.5.0 =` Upgrade Notice under 300 characters.
  </behavior>
  <action>
    Re-derive the list yourself with `git log --oneline v1.4.1..main` before
    writing — the table above is a snapshot and `main` may have moved.

    Write for a site owner, not a contributor: what can I now do, and what should
    I not assume it does. The single most important sentence is that hiding is
    cosmetic; someone who reads this as access control will misuse it as
    security. Put that in the changelog entry itself, not only in the FAQ.

    Mention that person-targeting needs `list_users`, since a site using
    `maestro_capability` will otherwise wonder where the picker went.

    Check the Upgrade Notice length explicitly (`awk`/`wc -c` on the line) —
    v1.4.0's came in at 266 of 300 and that was not accidental.
  </action>
  <verify>
    <automated>npm run check:doc-links</automated>
    <manual>Read the changelog aloud as a site owner who has never seen this plugin.</manual>
  </verify>
  <done>Changelog covers the real diff with the cosmetic-only guarantee and the multisite limitation stated; Upgrade Notice under 300 chars; no overclaims.</done>
</task>

</tasks>

<verification>
- `git diff` shows version strings changed ONLY via the script's touch points.
- The changelog names nothing that is not in `v1.4.1..main`.
- Upgrade Notice character count verified, not assumed.
</verification>

<success_criteria>
- Version strings at 1.5.0 everywhere.
- Changelog derived from the diff and free of overclaims.
- Cosmetic-only guarantee and multisite limitation both stated.
</success_criteria>

<output>
After completion, create `.planning/phases/26-release-v1.5.0/26-01-SUMMARY.md`.
</output>
