---
phase: 19-cosmetic-hiding-feasibility
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/19-cosmetic-hiding-feasibility/19-FEASIBILITY-NOTE.md
autonomous: false
requirements: [ROLE-01]

must_haves:
  truths:
    - "A reader can find a single, unambiguous go / no-go / partial-go verdict on whether per-user and cloned-role cosmetic hiding can stay strictly cosmetic within WordPress's role/user model"
    - "The verdict is argued by transfer from the SHIPPED per-role cosmetic proof (same resolution seam, wider match key) — not re-derived from first principles"
    - "If go/partial-go: the note names ONE recommended storage shape (from the three bounded options) and the exact Replay resolution seam it plugs into"
    - "If no-go for either branch: the note states the reason AND a concrete unblock trigger, and marks that branch deferred"
    - "The note contains a guardrail test SKETCH asserting current_user_can() is invariant before/after applying a user/profile rule (prose/pseudocode, not a runnable test)"
    - "The note honors every locked framing: cosmetic-only, no wp-sudo/external dependency, union-precedence additive hides, intersect-against-live-roles, sparse/non-destructive storage, minimum safety rails, single-site primary with multisite flagged"
    - "A human reviewer has signed off the note before Phase 21 planning may begin"
  artifacts:
    - path: ".planning/phases/19-cosmetic-hiding-feasibility/19-FEASIBILITY-NOTE.md"
      provides: "The ROLE-01 feasibility verdict, storage/seam recommendation, and guardrail test sketch"
      min_lines: 120
      contains: "Verdict"
  key_links:
    - from: "19-FEASIBILITY-NOTE.md verdict argument"
      to: "includes/class-replay.php:299 is_hidden_for_current_user()"
      via: "cites the shipped per-role drop-from-$menu seam as the transferable cosmetic proof"
      pattern: "is_hidden_for_current_user"
    - from: "19-FEASIBILITY-NOTE.md storage recommendation"
      to: "includes/class-config.php:178 sanitize() hidden_roles plumbing"
      via: "recommended storage axis reuses the sanitize/bound/sparse pattern"
      pattern: "hidden_roles|sanitize"
---

<objective>
Produce the ROLE-01 feasibility note: a written go / no-go / partial-go verdict on
whether per-user and cloned-role cosmetic menu hiding can be delivered WITHOUT
touching capabilities, plus (if go) the recommended storage shape and Replay
resolution seam, plus a guardrail test sketch. The note GATES Phase 21 (ROLE-02);
Phase 21 cannot begin planning without an explicit go verdict signed off here.

Purpose: De-risk Phase 21 before any code is written — prove the cosmetic-only
guarantee transfers to a wider match key, or explain why it can't and what would
unblock it.
Output: `.planning/phases/19-cosmetic-hiding-feasibility/19-FEASIBILITY-NOTE.md`
(analysis + guardrail test sketch only — NO plugin code, NO editor UI, NO new PHP).
</objective>

<execution_context>
@/Users/danknauss/.claude/get-shit-done/workflows/execute-plan.md
@/Users/danknauss/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/19-cosmetic-hiding-feasibility/19-CONTEXT.md

<interfaces>
<!-- The shipped per-role cosmetic hide IS the reference proof. These are the exact -->
<!-- seams the note argues FROM. Read them directly — no codebase exploration needed. -->

From includes/class-replay.php:299 — the cosmetic hide resolution seam (the whole
argument transfers from here; per-user/profile is "same seam, wider match key"):
```php
private function is_hidden_for_current_user( array $ovr ) {
    if ( empty( $ovr['hidden_roles'] ) ) {
        return false;
    }
    $user = wp_get_current_user();
    if ( ! $user || empty( $user->roles ) ) {
        return false;
    }
    return (bool) array_intersect( (array) $user->roles, (array) $ovr['hidden_roles'] );
}
```
- Called at class-replay.php:149 (top-level) and :190 (submenu); a true return DROPS
  the item from $menu/$submenu. It NEVER reads, grants, or removes a capability —
  this is exactly why the shipped hide is cosmetic. That property is what must transfer.

From includes/class-replay.php:391 — resolves a rendered slug's stored hide through
the normalized lookup (per-user/profile resolution must ride this SAME normalized path):
```php
private function resolved_hidden_roles( $slug, array $norm_items, array $norm_skip, $base ) {
    $nk = Slug::normalize( (string) $slug, $base );
    if ( '' === $nk || isset( $norm_skip[ $nk ] ) || ! isset( $norm_items[ $nk ]['hidden_roles'] ) ) {
        return array();
    }
    return $norm_items[ $nk ]['hidden_roles'];
}
```

From includes/class-config.php:178 — validation/bound plumbing a `hidden_users` /
profile axis would reuse (sparse: only stores non-empty; bounded by MAX_HIDDEN_ROLES=50):
```php
if ( ! empty( $item['hidden_roles'] ) && is_array( $item['hidden_roles'] ) ) {
    $roles = array_values(
        array_intersect( array_map( 'sanitize_key', $item['hidden_roles'] ), $valid_roles )
    );
    if ( count( $roles ) > self::MAX_HIDDEN_ROLES ) {
        $roles = array_slice( $roles, 0, self::MAX_HIDDEN_ROLES );
    }
    if ( $roles ) {
        $entry['hidden_roles'] = $roles;
    }
}
```
- Stored item shape (class-config.php:16): `items[slug] => { title?, icon?, hidden_roles?[] }`.
  Sparse-delta: reset = delete the key; config is never rewritten at resolve time.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Analyze the cosmetic-safety transfer + characterize storage/assignment options</name>
  <files>.planning/phases/19-cosmetic-hiding-feasibility/19-FEASIBILITY-NOTE.md</files>
  <action>
    Create the feasibility note and write its ANALYSIS core (the go/no-go engine).
    This is investigation + writing — do NOT write plugin code, a PoC, or a runnable test.

    Open with a title, date, and a one-line status placeholder ("Verdict: <filled in
    Task 2>", "Sign-off: pending"). Then write these analysis sections:

    1. **Reference proof (the anchor).** Restate the shipped per-role cosmetic hide as
       the transferable proof: `is_hidden_for_current_user()` (class-replay.php:299)
       returns a boolean via `array_intersect($user->roles, $ovr['hidden_roles'])`, and
       a true return DROPS the item from $menu/$submenu at :149/:190. It reads/grants/
       removes NO capability. State the cosmetic guarantee as a property of THIS seam.

    2. **Same seam, wider match key.** Frame per-user hiding and cloned-role (internal
       profile) hiding as the SAME drop-from-$menu mechanism with a wider match key:
       instead of intersecting only `$user->roles` against `hidden_roles`, also match a
       per-user-ID axis and/or a named-profile membership. Argue the cosmetic guarantee
       transfers BECAUSE the mechanism is unchanged (still a boolean gate on a drop, still
       zero capability reads) — do not re-derive cosmetic-safety from first principles.
       Show WHERE the widened check lives: widen `is_hidden_for_current_user()` (one
       drop path) and resolve through the SAME normalized lookup used by
       `resolved_hidden_roles()` (class-replay.php:391).

    3. **"Cloned role" definition + rejection of the WP-role-duplicate reading.** Define
       cloned role as a Maestro-INTERNAL named hiding profile (a named set of hides, e.g.
       "Reduced view") assigned cosmetically — NEVER `add_role()`/`add_cap()`/any
       capability-mutating WP API. Briefly document and REJECT the "real WP role duplicate"
       interpretation, stating why it would break the cosmetic bar (it would create/mutate
       capabilities), leaving an auditable rationale.

    4. **Invariants that must hold (the go bar).** State the go bar explicitly:
       `current_user_can()` must be provably UNCHANGED for any capability when a
       user/clone rule is applied or removed. Enumerate the locked model invariants the
       design must preserve: intersect-against-live-roles (self-healing; stale rules
       silently stop applying when roles change), union precedence (any applicable rule
       hides; additive; NO un-hide), and sparse/non-destructive storage (store only what's
       hidden; never rewrite config at resolve time; reset = delete). Tie each back to the
       shipped model so consistency is explicit.

    5. **Storage options (bounded) + assignment mechanism.** Characterize the three bounded
       storage options against the sanitize/bound/sparse constraints (class-config.php:178,
       MAX_HIDDEN_ROLES): (a) inline parallel axis `items[slug].hidden_users` alongside
       `hidden_roles`; (b) separate top-level map keyed by user ID / profile; (c) named-
       profile registry. For each: pros/cons re sparse-delta, reset=delete, sanitize reuse,
       and resolve-path fit. Also weigh the assignment mechanism (named-profile registry vs.
       flat per-user axis vs. unified target model). Do NOT pick the winner yet — Task 2
       writes the recommendation. Just lay out the tradeoffs so the recommendation is earned.

    Do NOT mention wp-sudo as a home/answer anywhere. Do NOT solve multisite here.
  </action>
  <verify>
    <automated>test -f .planning/phases/19-cosmetic-hiding-feasibility/19-FEASIBILITY-NOTE.md && grep -q "is_hidden_for_current_user" .planning/phases/19-cosmetic-hiding-feasibility/19-FEASIBILITY-NOTE.md && grep -q "hidden_users\|named-profile\|profile registry" .planning/phases/19-cosmetic-hiding-feasibility/19-FEASIBILITY-NOTE.md && ! grep -qi "wp-sudo" .planning/phases/19-cosmetic-hiding-feasibility/19-FEASIBILITY-NOTE.md</automated>
  </verify>
  <done>
    Note file exists with the five analysis sections: the reference proof cites
    class-replay.php:299, the transfer argument is framed as "same seam, wider match key",
    cloned-role is defined as internal-profile-only with the WP-role-duplicate reading
    rejected, the current_user_can() go bar and the intersect/union/sparse invariants are
    stated, and all three bounded storage options are characterized with tradeoffs. No
    wp-sudo reference anywhere in the file.
  </done>
</task>

<task type="auto">
  <name>Task 2: Write the verdict, recommendations, guardrail test sketch, and framing sections</name>
  <files>.planning/phases/19-cosmetic-hiding-feasibility/19-FEASIBILITY-NOTE.md</files>
  <action>
    Complete the note by writing the decision + supporting sections and filling the
    status placeholder from Task 1. Still analysis/writing only — no code, no runnable test.

    1. **Verdict.** State a single, unambiguous verdict: go, no-go, or partial-go
       (per-user vs. cloned-role may differ — partial-go is allowed). Fill the header
       status line. The verdict MUST follow from Task 1's analysis. Because both branches
       reuse the shipped drop-from-$menu seam with zero capability reads, the expected
       verdict is go (or partial-go); if the analysis surfaces a genuine blocker for one
       branch, mark THAT branch no-go with a reason and a concrete unblock trigger (a WP
       capability-model change, a design bridge, etc.) — the deferral must stay actionable.

    2. **Recommendation (storage + assignment + resolution seam).** Recommend ONE of the
       three bounded storage options and ONE assignment mechanism, justified against the
       sparse/reset/sanitize constraints from Task 1. State the resolution seam concretely:
       widen `is_hidden_for_current_user()` (class-replay.php:299) — one drop-from-$menu
       path — resolving through the SAME normalized lookup as `resolved_hidden_roles()`
       (:391). Note the sanitize/bound reuse at class-config.php:178.

    3. **Guardrail test SKETCH.** Write a prose/pseudocode sketch (NOT runnable) of the
       test Phase 21 will implement for real: capture current_user_can() for a
       representative capability set for a user, apply a user/profile hide rule, assert
       current_user_can() is byte-for-byte unchanged, remove the rule, assert unchanged
       again. Make explicit this asserts the cosmetic INVARIANT (visibility changed, access
       did not). Label it clearly as a sketch that Phase 21 implements.

    4. **Need/value.** Short section: when per-user beats the shipped per-role hide (e.g.
       decluttering for one specific admin) — so Phase 21 isn't built on an untested
       assumption.

    5. **Coexistence with the shipped per-role hide.** Recommend how the additive
       user/profile layer surfaces in the existing visibility popover (role checkboxes)
       WITHOUT breaking the current "hide for role X" mental model or the config shape.
       Union precedence already locked.

    6. **Targeting UX feasibility flag (options, not a decision).** List candidate targeting
       affordances (async user-search picker vs. dropdown vs. hide-for-this-user-while-
       viewing) and flag the large-user-count perf risk of loading all users. Explicitly
       leave the pick to Phase 21.

    7. **Safety rails (minimum).** Require, at minimum: never hide the Maestro editor entry
       / admin-bar toggle for the acting admin; and document that hidden pages remain
       URL-reachable (the escape hatch).

    8. **Multisite risk flag.** Single-site is primary; call out multisite specifics (super
       admin, per-site roles, network users) as a known risk/edge for Phase 21 — do NOT
       solve here.

    9. **Enforcement-line reaffirmation.** Short section: ROLE-02 is cosmetic-only, full
       stop; Maestro NEVER enforces (WordPress's per-page capability check is the true gate;
       a hidden page still loads by URL for a capable user). Maestro assumes NO dependency
       on any other plugin — the cosmetic-only guarantee holds on Maestro alone. wp-sudo /
       any enforced tier is an internal-roadmap note only with NO functional reference here.

    Close by confirming this note gates Phase 21 and that Phase 21 planning requires an
    explicit signed-off go verdict (the Task 3 checkpoint).
  </action>
  <verify>
    <automated>grep -qi "Verdict" .planning/phases/19-cosmetic-hiding-feasibility/19-FEASIBILITY-NOTE.md && grep -qi "current_user_can" .planning/phases/19-cosmetic-hiding-feasibility/19-FEASIBILITY-NOTE.md && grep -qi "sketch" .planning/phases/19-cosmetic-hiding-feasibility/19-FEASIBILITY-NOTE.md && grep -qi "URL-reachable\|loads by URL\|reachable by URL\|reachable by direct URL" .planning/phases/19-cosmetic-hiding-feasibility/19-FEASIBILITY-NOTE.md && grep -qi "multisite" .planning/phases/19-cosmetic-hiding-feasibility/19-FEASIBILITY-NOTE.md && ! grep -qi "wp-sudo" .planning/phases/19-cosmetic-hiding-feasibility/19-FEASIBILITY-NOTE.md</automated>
  </verify>
  <done>
    Note now states a clear go/no-go/partial-go verdict (header status filled) that follows
    from Task 1's analysis; recommends one storage shape + assignment mechanism + the
    widened is_hidden_for_current_user() seam; contains a labeled non-runnable guardrail
    test sketch asserting the current_user_can() invariant; and includes the need/value,
    coexistence, targeting-UX-options, safety-rails, multisite-risk, and enforcement-line
    sections. If any branch is no-go, its reason + unblock trigger is stated. No wp-sudo
    dependency framing anywhere.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Feasibility note review and sign-off gate</name>
  <action>Human reviewer reads 19-FEASIBILITY-NOTE.md end to end and signs off (or requests changes) before Phase 21 planning may begin. This is the phase gate — see what-built/how-to-verify below.</action>
  <what-built>
    The ROLE-01 feasibility note is complete at
    .planning/phases/19-cosmetic-hiding-feasibility/19-FEASIBILITY-NOTE.md — a written
    go/no-go/partial-go verdict on cosmetic per-user + cloned-role hiding, a recommended
    storage shape and Replay resolution seam, a guardrail test sketch, and the coexistence /
    UX / safety-rails / multisite / enforcement-line sections. This checkpoint is phase
    success criterion #4: the note must be reviewed and signed off BEFORE Phase 21 planning
    may begin.
  </what-built>
  <how-to-verify>
    1. Read .planning/phases/19-cosmetic-hiding-feasibility/19-FEASIBILITY-NOTE.md end to end.
    2. Confirm the verdict is clear and unambiguous (go / no-go / partial-go), and that it
       actually follows from the analysis (the "same seam, wider match key" transfer from
       the shipped per-role proof).
    3. If go/partial-go: confirm ONE storage shape and the widened
       is_hidden_for_current_user() seam are named, and the guardrail test sketch asserts
       the current_user_can() invariant.
    4. If any branch is no-go: confirm the reason AND a concrete unblock trigger are stated.
    5. Confirm the locked framing holds: cosmetic-only, NO wp-sudo/external dependency,
       union-precedence additive hides, intersect-against-live-roles, sparse/non-destructive
       storage, minimum safety rails (never hide the acting admin's editor entry; hidden
       pages stay URL-reachable), single-site primary with multisite flagged.
    6. Decide: does this verdict clear the cosmetic-only bar well enough to authorize Phase 21
       planning (or partial-go authorize the clean branch and defer the other)?
  </how-to-verify>
  <resume-signal>
    Type "approved" (optionally noting go / partial-go / no-go and which branch) to sign off,
    or describe required changes to the note.
  </resume-signal>
</task>

</tasks>

<verification>
Maps to the 4 phase success criteria (ROADMAP Phase 19):
1. Clear go/no-go verdict on cosmetic-only feasibility → Task 2 Verdict section + Task 3 review.
2. If go: storage shape + Replay resolution seam specified → Task 2 Recommendation section.
3. If no-go: reason + (per locked decision) unblock trigger, branch marked deferred → Task 2 Verdict.
4. Reviewed + signed off before Phase 21 planning → Task 3 blocking human-verify checkpoint.

Cross-cutting guardrail check: `grep -i "wp-sudo" 19-FEASIBILITY-NOTE.md` returns nothing
(no external-dependency framing); the note argues the cosmetic guarantee holds on Maestro alone.
</verification>

<success_criteria>
- 19-FEASIBILITY-NOTE.md exists with a single unambiguous go/no-go/partial-go verdict.
- Verdict argued by transfer from the shipped per-role proof (is_hidden_for_current_user seam),
  not re-derived from scratch.
- If go/partial-go: one recommended storage shape + the widened Replay seam + a guardrail
  test sketch asserting the current_user_can() invariant are present.
- If no-go (any branch): reason + concrete unblock trigger stated; branch marked deferred.
- All locked framings honored (cosmetic-only, no wp-sudo dependency, union/intersect/sparse,
  safety rails, single-site primary + multisite risk flag).
- Human sign-off recorded via the blocking checkpoint.
</success_criteria>

<output>
After completion, create
`.planning/phases/19-cosmetic-hiding-feasibility/19-01-SUMMARY.md` recording the verdict
(go / partial-go / no-go per branch), the recommended storage shape + resolution seam (if go),
any deferred branch with its unblock trigger, and the sign-off outcome — so Phase 21 planning
can read the gate result directly.
</output>
