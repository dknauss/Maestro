---
phase: 18-release-v1.3.0
plan: 03
type: execute
wave: 3
depends_on: ["18-02"]
files_modified: []
autonomous: false
requirements: [REL-09]
must_haves:
  truths:
    - "release.yml completed green: the GitHub Release for v1.3.0 is published with maestro-menu-editor.zip attached"
    - "wp-deploy.yml completed green: the SVN deploy job succeeded (trunk updated + 1.3.0 SVN tag cut + assets staged)"
    - "Plugin Check reports 0 errors on the built runtime zip and the e2e/integration suites were green at the release commit (carried from the green CI gate)"
  artifacts:
    - path: ".github/workflows/release.yml"
      provides: "Builds build/maestro-menu-editor.zip via bin/build.sh and publishes the GitHub Release with auto-generated notes"
    - path: ".github/workflows/wp-deploy.yml"
      provides: "On release:published → 10up action-wordpress-plugin-deploy → SVN trunk + 1.3.0 tag + .wordpress-org assets"
  key_links:
    - from: "release.yml (push: tags v1.3.0)"
      to: "GitHub Release with zip asset"
      via: "softprops/action-gh-release publishes zip + generated notes"
      pattern: "maestro-menu-editor.zip"
    - from: "GitHub Release published event"
      to: "wp-deploy.yml run"
      via: "release: types [published] auto-triggers SVN deploy"
      pattern: "wporg-deploy"
    - from: "wp-deploy.yml (10up action)"
      to: "WordPress.org SVN trunk + 1.3.0 tag"
      via: "WP_ORG_SVN_USERNAME / WP_ORG_SVN_PASSWORD secrets"
      pattern: "1\\.3\\.0"
---

<objective>
Verify the v1.3.0 release landed: confirm `release.yml` published the GitHub Release with the
runtime zip, and `wp-deploy.yml` deployed to WordPress.org SVN — the REL-09 "done" bar. Then
flip ROADMAP/REQUIREMENTS traceability and STATE to reflect the shipped release.

Purpose: The 18-02 tag push fires both workflows automatically. This plan is the confirmation
that the established v1.2.0 pipeline ran end-to-end green for v1.3.0, plus closing out the
phase records.

Output: Confirmed green release.yml + wp-deploy.yml; REL-09 marked Complete; Phase 18 marked
done; STATE updated. No new tests — the regression validation was the green CI gate confirmed in
18-02.

CONFIRMATION-GATED: this plan has no network-mutating actions of its own, but the deploy it
verifies is irreversible (a published wp.org version can't be cleanly un-published). The
verification checkpoint pauses for the human to confirm both workflows are green before the phase
is recorded complete. If a workflow is red, follow the fix-forward posture (below) — do NOT
record completion.
</objective>

<execution_context>
@/Users/danknauss/.claude/get-shit-done/workflows/execute-plan.md
@/Users/danknauss/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/18-release-v1.3.0/18-CONTEXT.md
@.planning/phases/18-release-v1.3.0/18-02-SUMMARY.md

<done_bar>
REL-09 / Phase 18 done bar (locked in 18-CONTEXT.md): both `release.yml` and `wp-deploy.yml`
complete GREEN — the zip asset is attached to the GitHub Release AND the deploy job succeeds. The
SVN `trunk` / `1.3.0` SVN tag and the live wordpress.org listing showing 1.3.0 follow
automatically and may be spot-checked, but are CONFIRMATORY, not gating (the live listing can lag
the deploy by minutes–hours).

ROADMAP Phase 18 success criteria covered here:
  1. bin/build.sh produces a clean zip + Plugin Check 0 errors on the extracted zip
     (built/checked inside CI on WP 7.0; release.yml builds the published zip).
  2. PHP unit + integration + Playwright e2e green at the release commit, no skips
     (carried from the green ci.yml gate confirmed in 18-02).
  3. git tag v1.3.0 exists + GitHub Release published (release.yml green).
  4. SVN trunk updated + 1.3.0 SVN tag cut following the v1.2.0 pipeline (wp-deploy.yml green).
</done_bar>

<failure_posture>
Fix-forward, never partial-publish:
  - release.yml red: diagnose, fix-forward on a new commit, re-tag only when fully green —
    never leave a half-published release.
  - wp-deploy.yml red: the 10up action is idempotent — re-run wp-deploy.yml for the SAME tag
    (workflow_dispatch with tag input v1.3.0, or re-run the failed job). If SVN was left
    partially updated, fix-forward with a 1.3.1 rather than rewriting history or un-publishing.
No manual SVN-rollback procedure; a hotfix (1.3.x) is the real recovery path.
</failure_posture>

<tooling_note>
`gh` API calls may fail under the restricted network sandbox (TLS cert errors at plan time). If
`gh run list` / `gh run view` fail, fall back to the GitHub web UI Actions tab and Releases page
as authoritative; report status to the human from there.
</tooling_note>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Confirm release.yml + wp-deploy.yml both green</name>
  <action>Pause for the user. Surface the release.yml and wp-deploy.yml run status shown under how-to-verify, then wait for the resume-signal before recording phase completion. If either workflow is red, follow the fix-forward posture and do not approve.</action>
  <what-built>
    The v1.3.0 tag push (18-02) fired release.yml, which builds maestro-menu-editor.zip and
    publishes the GitHub Release; publishing then auto-fired wp-deploy.yml to deploy to SVN. The
    executor surfaces both workflow runs for confirmation.
  </what-built>
  <how-to-verify>
    Executor attempts (and on sandbox failure, points the human to the web UI):
      - `gh run list --workflow=release.yml --limit 3` and `gh run list --workflow=wp-deploy.yml --limit 3`
      - or GitHub → Actions tab.
    Confirm ALL of:
      1. release.yml run for tag v1.3.0 completed GREEN — "Verify tag matches plugin versions"
         passed (tag==header==Stable tag), zip built, Release published.
      2. The GitHub Release page for v1.3.0 shows `maestro-menu-editor.zip` attached and
         auto-generated notes.
      3. wp-deploy.yml run (triggered by release:published) completed GREEN — the deploy job
         succeeded.
      4. (Confirmatory, non-gating) spot-check SVN: trunk updated and a `1.3.0` SVN tag exists;
         the wp.org listing shows 1.3.0 (may lag — fine if not yet visible).
    If any of 1–3 is red, STOP and follow the fix-forward posture in &lt;failure_posture&gt;;
    do not approve.
  </how-to-verify>
  <resume-signal>Type "release verified" once release.yml AND wp-deploy.yml are both green with the zip attached, or describe the red workflow to fix-forward.</resume-signal>
</task>

<task type="auto">
  <name>Task 1: Mark REL-09 / Phase 18 complete and update traceability + STATE</name>
  <files>.planning/REQUIREMENTS.md, .planning/ROADMAP.md, .planning/STATE.md</files>
  <action>
    Only after "release verified":
      - REQUIREMENTS.md: flip REL-09 checkbox to `[x]` and set its Traceability row status to
        `Complete`. Update the Coverage note if it tracks shipped counts.
      - ROADMAP.md: mark the Phase 18 row Complete with today's date in the Progress table; if
        the milestone line for v1.3.0 records a ship date, set it (shipped today, release tag
        v1.3.0).
      - STATE.md: advance Current Position / status to reflect v1.3.0 SHIPPED (Phase 18 complete,
        2/2 phases); update the milestone progress bar; append a decision/blocker note recording
        the green release.yml + wp-deploy.yml runs (and any SVN/live-listing spot-check result).
    Keep edits factual and minimal — this is record-keeping, not narrative.
  </action>
  <verify>
    <automated>grep -q '\[x\] \*\*REL-09\*\*' .planning/REQUIREMENTS.md &amp;&amp; grep -q 'REL-09' .planning/REQUIREMENTS.md &amp;&amp; grep -qi 'v1.3.0' .planning/STATE.md</automated>
  </verify>
  <done>REL-09 marked Complete in REQUIREMENTS.md traceability; Phase 18 Complete in ROADMAP; STATE reflects v1.3.0 shipped.</done>
</task>

</tasks>

<verification>
Observable release-state checks (no new unit tests — the regression validation was the green CI
gate confirmed in 18-02):
- release.yml green: GitHub Release v1.3.0 published with maestro-menu-editor.zip attached.
- wp-deploy.yml green: SVN deploy job succeeded (trunk + 1.3.0 tag + assets).
- Confirmatory: SVN trunk / 1.3.0 tag present; wp.org listing shows 1.3.0 (may lag).
- REL-09 + Phase 18 recorded Complete; STATE reflects shipped.
</verification>

<success_criteria>
- ROADMAP success-criterion 1: clean zip + Plugin Check 0 errors (built/verified in CI + release.yml)
- ROADMAP success-criterion 2: unit/integration/e2e green at release commit (carried from CI gate)
- ROADMAP success-criterion 3: v1.3.0 tag exists + GitHub Release published (release.yml green)
- ROADMAP success-criterion 4: SVN trunk + 1.3.0 tag cut via the v1.2.0 pipeline (wp-deploy.yml green)
- REL-09 marked Complete; Phase 18 marked done; STATE updated
</success_criteria>

<output>
After completion, create `.planning/phases/18-release-v1.3.0/18-03-SUMMARY.md`
</output>
