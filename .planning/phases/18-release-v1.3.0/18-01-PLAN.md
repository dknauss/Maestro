---
phase: 18-release-v1.3.0
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - maestro-menu-editor.php
  - readme.txt
  - playground/blueprint-stable.json
autonomous: true
requirements: [REL-09]
must_haves:
  truths:
    - "Plugin header Version, MAESTRO_VERSION, Stable tag, and blueprint ref all read 1.3.0 / v1.3.0"
    - "readme.txt == Upgrade Notice == has a = 1.3.0 = entry with the locked reliability copy"
    - "The version-bump is the final commit on gsd/phase-17-slug-normalization so one PR carries FIX code + bump"
  artifacts:
    - path: "maestro-menu-editor.php"
      provides: "Plugin header Version: 1.3.0 + MAESTRO_VERSION '1.3.0' define"
      contains: "1.3.0"
    - path: "readme.txt"
      provides: "Stable tag: 1.3.0 + Upgrade Notice = 1.3.0 = entry"
      contains: "= 1.3.0 ="
    - path: "playground/blueprint-stable.json"
      provides: "installPlugin ref v1.3.0"
      contains: "v1.3.0"
  key_links:
    - from: "bin/prep-release.sh 1.3.0"
      to: "maestro-menu-editor.php + readme.txt + playground/blueprint-stable.json"
      via: "atomic version bump + php -l guard"
      pattern: "Version bumped to v1.3.0"
    - from: "git tag verification (release.yml)"
      to: "header Version == Stable tag == tag"
      via: "all three must equal 1.3.0 by construction"
      pattern: "1\\.3\\.0"
---

<objective>
Bump every shipped version string to 1.3.0 and add the locked `= 1.3.0 =` Upgrade Notice
entry, landing it as the FINAL commit on `gsd/phase-17-slug-normalization` so a single PR
carries both the FIX-01/02/03 slug code and the release metadata.

Purpose: `release.yml` verifies `tag == header Version == Stable tag` before building. Bumping
all four version strings atomically (via `bin/prep-release.sh`) plus the manual Upgrade Notice
makes that check pass by construction on the eventual v1.3.0 merge commit.

Output: A clean version-bump commit on the release branch. No outward-facing actions in this
plan — push/merge/tag happen in 18-02 behind confirmation gates.
</objective>

<execution_context>
@/Users/danknauss/.claude/get-shit-done/workflows/execute-plan.md
@/Users/danknauss/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-release-v1.3.0/18-CONTEXT.md

<release_pipeline_note>
This is a PURE RELEASE phase. No new plugin business logic, no new translatable strings, no
new tests. The slug-normalization payload (FIX-01/02/03) already shipped in Phase 17 and is
already on this branch. Per the Nyquist decision "Continue — CI is the validation": there is
NO VALIDATION.md and test-coverage validation is N/A. The regression validation for this phase
is the EXISTING green CI gate (ci.yml) on the release PR plus the green release workflows
(release.yml, wp-deploy.yml) — NOT new unit tests. Do not add or expect new test coverage.
</release_pipeline_note>

<bump_mechanics>
`bin/prep-release.sh <version>` (no leading v) atomically updates and `php -l`-guards:
  - maestro-menu-editor.php   `* Version:` header + `MAESTRO_VERSION` define
  - readme.txt                `Stable tag:`
  - playground/blueprint-stable.json   installPlugin step `"ref": "v<version>"`

It does NOT touch the readme `== Upgrade Notice ==` section — that edit is manual.

Current state (verified at plan time): all four read 1.2.0 / v1.2.0. readme.txt
`== Changelog ==` already has a `= 1.3.0 =` entry (committed in Phase 17). `== Upgrade Notice ==`
stops at `= 1.2.0 =` and must gain a `= 1.3.0 =` entry.
</bump_mechanics>

<locked_upgrade_notice_copy>
Exact text to insert as the new first entry under `== Upgrade Notice ==` (locked in 18-CONTEXT.md):

= 1.3.0 =
Reliability fix: your saved menu overrides keep applying after a site moves hosts, a plugin update changes a menu URL's version number, tracking parameters drift, or a slug's `&` is stored as `&amp;`. No configuration changes required.
</locked_upgrade_notice_copy>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run prep-release.sh 1.3.0 to bump all four version strings</name>
  <files>maestro-menu-editor.php, readme.txt, playground/blueprint-stable.json</files>
  <action>
    Confirm the working tree is clean and the current branch is `gsd/phase-17-slug-normalization`
    (`git status`, `git branch --show-current`). If not on that branch, STOP — this plan must
    commit onto the release branch.

    Run `bash bin/prep-release.sh 1.3.0`. This bumps the plugin header `* Version:` to 1.3.0,
    the `MAESTRO_VERSION` define to '1.3.0', readme `Stable tag:` to 1.3.0, and the
    blueprint-stable.json installPlugin `"ref"` to "v1.3.0", then runs `php -l` on the plugin
    file. Do NOT hand-edit these four strings — the script is the single source of truth and
    guards against header corruption.

    The script prints "Version bumped to v1.3.0 in: ..." on success. Its printed "Next steps"
    (open a release/ branch, etc.) are SUPERSEDED by 18-CONTEXT.md's locked one-PR-on-this-branch
    strategy — ignore them; do not create a new branch.
  </action>
  <verify>
    <automated>grep -q '^ \* Version:           1.3.0' maestro-menu-editor.php && grep -q "define( 'MAESTRO_VERSION', '1.3.0' )" maestro-menu-editor.php && grep -q '^Stable tag: 1.3.0' readme.txt && grep -q '"ref": "v1.3.0"' playground/blueprint-stable.json && php -l maestro-menu-editor.php</automated>
  </verify>
  <done>All four version strings read 1.3.0 / v1.3.0; plugin file passes php -l.</done>
</task>

<task type="auto">
  <name>Task 2: Add the locked = 1.3.0 = Upgrade Notice entry</name>
  <files>readme.txt</files>
  <action>
    In readme.txt, locate the `== Upgrade Notice ==` heading. Insert a new `= 1.3.0 =` entry as
    the FIRST entry (immediately above the existing `= 1.2.0 =` entry), using the locked copy
    verbatim from &lt;locked_upgrade_notice_copy&gt; above:

    = 1.3.0 =
    Reliability fix: your saved menu overrides keep applying after a site moves hosts, a plugin update changes a menu URL's version number, tracking parameters drift, or a slug's `&` is stored as `&amp;`. No configuration changes required.

    Follow one blank line after the entry's body (match the existing entry spacing). Do NOT alter
    the existing `= 1.2.0 =` / `= 1.1.1 =` / `= 1.1.0 =` entries. Do NOT touch `== Changelog ==`
    (its `= 1.3.0 =` entry already exists from Phase 17). Preserve the literal backticks and the
    `&amp;` as written — this is readme.txt markup, not HTML to escape.
  </action>
  <verify>
    <automated>awk '/== Upgrade Notice ==/{u=1} u && /= 1.3.0 =/{found=1} u && /No configuration changes required\./{body=1} END{exit !(found &amp;&amp; body)}' readme.txt</automated>
  </verify>
  <done>== Upgrade Notice == has a = 1.3.0 = entry ending "No configuration changes required."; existing entries unchanged.</done>
</task>

<task type="auto">
  <name>Task 3: Commit the version bump as the final commit on the release branch</name>
  <files>maestro-menu-editor.php, readme.txt, playground/blueprint-stable.json</files>
  <action>
    Stage exactly the three bumped files plus readme.txt and commit with a release-bump message
    following the project convention `{type}({scope}): {description}`. Suggested:
    `chore(release): bump version to 1.3.0 and add upgrade notice`.

    This is intentionally the LAST commit on `gsd/phase-17-slug-normalization` before the PR —
    do not commit any planning artifacts (PLAN.md / SUMMARY.md) in this same commit (those are
    committed separately by the GSD workflow). Do NOT push in this plan — pushing is a
    confirmation-gated step in 18-02.

    Run `git log --oneline -1` and confirm the bump commit is HEAD.
  </action>
  <verify>
    <automated>git diff --quiet && git diff --cached --quiet && git log -1 --pretty=%s | grep -qi '1.3.0'</automated>
  </verify>
  <done>Version-bump commit is HEAD on gsd/phase-17-slug-normalization; working tree clean; nothing pushed.</done>
</task>

</tasks>

<verification>
Observable release-state checks (no new unit tests — see release_pipeline_note):
- All four version strings read 1.3.0 / v1.3.0 (grep checks above).
- readme.txt Upgrade Notice has the locked = 1.3.0 = entry.
- `php -l maestro-menu-editor.php` passes.
- The bump is a single clean commit at HEAD; nothing pushed yet.
</verification>

<success_criteria>
- maestro-menu-editor.php header Version + MAESTRO_VERSION = 1.3.0
- readme.txt Stable tag = 1.3.0 AND Upgrade Notice = 1.3.0 = entry present
- playground/blueprint-stable.json ref = v1.3.0
- Single bump commit at HEAD of the release branch; no push performed
</success_criteria>

<output>
After completion, create `.planning/phases/18-release-v1.3.0/18-01-SUMMARY.md`
</output>
