---
phase: 18-release-v1.3.0
plan: 02
type: execute
wave: 2
depends_on: ["18-01"]
files_modified: []
autonomous: false
requirements: [REL-09]
must_haves:
  truths:
    - "The release PR (one PR on gsd/phase-17-slug-normalization) carries FIX code + version bump and shows all CI checks green"
    - "The PR is merged into main, producing a merge commit that contains class-slug.php + replay wiring + 1.3.0 version strings"
    - "The git tag v1.3.0 exists on the main merge commit and is pushed to origin"
  artifacts:
    - path: ".github/workflows/ci.yml"
      provides: "Authoritative green gate (unit/JS/WPCS/PHPStan/audits + integration+e2e+Plugin Check on WP 7.0) — green on the PR is the validation"
    - path: ".github/workflows/release.yml"
      provides: "Fires on push of tag v* — verifies tag==header==Stable tag, builds zip, publishes Release (verified in 18-03)"
  key_links:
    - from: "git push origin gsd/phase-17-slug-normalization"
      to: "PR #65 CI run (ci.yml)"
      via: "push triggers full gate"
      pattern: "All checks have passed"
    - from: "PR merge into main"
      to: "git tag v1.3.0 on the merge commit"
      via: "tag points at a main commit containing the slug code + 1.3.0 strings"
      pattern: "v1.3.0"
    - from: "git push origin v1.3.0 (tag)"
      to: "release.yml trigger"
      via: "push: tags v* fires release build"
      pattern: "v1.3.0"
---

<objective>
Execute the outward-facing release actions: push the bumped release branch, confirm CI is green
on the PR, self-merge into `main`, then tag `v1.3.0` on the merge commit and push the tag.

Purpose: This produces the single clean `main` commit (PR merge) carrying both the FIX-01/02/03
slug code and the 1.3.0 version metadata, then the `v1.3.0` tag that fires `release.yml`. The
v1.3.0 tag MUST point at a `main` commit that contains class-slug.php and the replay wiring —
which today exist only on the feature branch — so the merge must precede the tag.

Output: PR merged to main; `v1.3.0` tag on the merge commit, pushed to origin. The release and
deploy workflows fire automatically from the tag and are VERIFIED in 18-03.

CONFIRMATION-GATED: every outward action in this plan (push, merge, tag push) pauses for explicit
user authorization. The executor MUST NOT push, merge, or push tags without the user typing
approval at each gate. Solo repo (dknauss/Maestro), self-merge is the model — but the human still
authorizes each network-mutating step.
</objective>

<execution_context>
@/Users/danknauss/.claude/get-shit-done/workflows/execute-plan.md
@/Users/danknauss/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-release-v1.3.0/18-CONTEXT.md
@.planning/phases/18-release-v1.3.0/18-01-SUMMARY.md

<release_pipeline_note>
PURE RELEASE phase — no new tests. Per the Nyquist decision "Continue — CI is the validation":
there is NO VALIDATION.md and test-coverage validation is N/A. The regression validation is the
EXISTING green ci.yml gate on the release PR. Do not expect or add new test coverage. CI green =
gate satisfied (per "gates enforce, reviewers judge" — no separate manual local full-suite run
required).
</release_pipeline_note>

<known_state>
At plan time: branch `gsd/phase-17-slug-normalization` is 16 commits ahead of main, 0 behind,
working tree clean. PR #65 (dknauss/Maestro) is open against main; all 13 CI checks were GREEN
at the pre-bump commit (including "Integration + E2E (wp-env, WP 7.0)" and "Runtime package +
Plugin Check", 0 errors). 18-01 added one more commit (the version bump), which re-triggers CI —
that fresh run must be confirmed green before merge.

Note on tooling: `gh` API calls may fail under a restricted network sandbox (TLS cert errors
were seen at plan time). If `gh` fails, fall back to the GitHub web UI for PR status/merge, or
re-run with network access; treat the web UI as authoritative for the green bar.
</known_state>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Confirm bump commit + authorize branch push</name>
  <action>Pause for the user. Present the working-tree / commit state shown under how-to-verify, then wait for the resume-signal before proceeding to the push task. Take no network action.</action>
  <what-built>
    18-01 produced the 1.3.0 version-bump commit at HEAD of `gsd/phase-17-slug-normalization`.
    Before any network action, the executor will run `git status`, `git log --oneline -3`, and
    confirm: working tree clean, bump commit is HEAD, branch is the release branch.
  </what-built>
  <how-to-verify>
    1. Executor runs `git status` (expect: clean) and `git log --oneline -3` (expect: the
       1.3.0 bump commit on top of the Phase 17 commits).
    2. Confirm this branch is the one PR #65 tracks.
    Then authorize the push.
  </how-to-verify>
  <resume-signal>Type "push approved" to push the branch, or describe what to fix first.</resume-signal>
</task>

<task type="auto">
  <name>Task 1: Push the release branch to origin (after push-approved gate)</name>
  <files></files>
  <action>
    Only after the push-approved signal: run `git push origin gsd/phase-17-slug-normalization`.
    This updates PR #65 with the version-bump commit and re-triggers ci.yml.

    Then surface the PR + CI status for the human: prefer `gh pr checks 65` and
    `gh pr view 65 --json statusCheckRollup,mergeable,state`. If `gh` fails under the sandbox
    (TLS/network), report that and point the human to the PR #65 "Checks" tab in the GitHub web
    UI as the authoritative status. Do NOT merge in this task — merge is the next gated step.
  </action>
  <verify>
    <automated>git rev-parse HEAD &amp;&amp; git ls-remote --heads origin gsd/phase-17-slug-normalization | grep -q "$(git rev-parse HEAD)"</automated>
  </verify>
  <done>Release branch pushed; remote tip == local HEAD; CI re-running on PR #65; status surfaced to human.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Confirm PR CI green + authorize merge</name>
  <action>Pause for the user. Present the PR #65 CI status shown under how-to-verify, then wait for the resume-signal before merging. Take no merge action until approved.</action>
  <what-built>
    PR #65 now carries the full v1.3.0 payload (FIX-01/02/03 + version bump). CI (ci.yml) is
    re-running the entire authoritative gate on the bumped commit.
  </what-built>
  <how-to-verify>
    1. Open PR #65 → "Checks". Confirm ALL checks are green, specifically:
       - PHP unit (matrix), JS unit, WPCS (composer lint), PHPStan, composer + npm audits
       - "Integration + E2E (wp-env, WP 7.0)"
       - "Runtime package + Plugin Check" (0 errors)
    2. Confirm there are no skips masking failures (success-criterion 2).
    3. Confirm the PR is mergeable (no conflicts; 0 behind main).
    Per the locked decision, this green CI run is the regression gate — no separate local
    full-suite run is required.
  </how-to-verify>
  <resume-signal>Type "merge approved" once all CI checks are green, or describe the red check to fix-forward.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Self-merge the PR into main (after merge-approved gate)</name>
  <files></files>
  <action>
    Only after merge-approved: merge PR #65 into `main`. Prefer
    `gh pr merge 65 --merge` (a merge commit keeps the v1.3.0 tag on one clean commit that
    contains both the fix and the version metadata, matching the locked strategy). If `gh` is
    blocked by the sandbox, the human performs the merge via the GitHub web UI "Merge pull
    request" button and tells the executor when done.

    After merge: `git fetch origin` and `git checkout main && git pull --ff-only origin main`.
    Confirm the merge commit is on main and that main now contains the slug code and 1.3.0
    strings:
      - `git grep -l "class Slug" -- includes/` (or confirm includes/class-slug.php present)
      - `grep '^ \* Version:           1.3.0' maestro-menu-editor.php`
    NEVER tag a commit that lacks class-slug.php or the replay wiring.
  </action>
  <verify>
    <automated>git checkout main &amp;&amp; git pull --ff-only origin main &amp;&amp; test -f includes/class-slug.php &amp;&amp; grep -q '^ \* Version:           1.3.0' maestro-menu-editor.php &amp;&amp; grep -q '^Stable tag: 1.3.0' readme.txt</automated>
  </verify>
  <done>PR merged to main; local main fast-forwarded; main contains includes/class-slug.php and 1.3.0 version strings.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Confirm main contains slug code + authorize tag</name>
  <action>Pause for the user. Present the main-branch verification shown under how-to-verify, then wait for the resume-signal before tagging. Create no tag until approved.</action>
  <what-built>
    main now contains the full v1.3.0 payload at the merge commit (slug code + version metadata
    verified in Task 2). The next action — tagging and pushing — is what fires release.yml.
  </what-built>
  <how-to-verify>
    1. Executor shows `git log --oneline -1` on main (the merge commit) and re-confirms
       `includes/class-slug.php` exists and the header reads Version 1.3.0.
    2. Confirm there is no pre-existing `v1.3.0` tag (`git tag -l v1.3.0` is empty;
       `git ls-remote --tags origin v1.3.0` is empty).
    Then authorize tagging + pushing the tag.
  </how-to-verify>
  <resume-signal>Type "tag approved" to create and push v1.3.0, or describe what to fix first.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Tag v1.3.0 on main and push the tag (after tag-approved gate)</name>
  <files></files>
  <action>
    Only after tag-approved, on the merge commit on `main`:
      1. `git tag v1.3.0` (annotated optional; lightweight matches the v1.2.0 precedent —
         use `git tag -a v1.3.0 -m "v1.3.0"` if the prior release used annotated tags, else
         plain `git tag v1.3.0`).
      2. `git push origin v1.3.0`.

    Pushing the tag fires `release.yml` (push: tags v*), which verifies
    `tag == header Version == Stable tag` (all 1.3.0 by construction from 18-01), builds the
    zip, and publishes the GitHub Release. That publish auto-triggers `wp-deploy.yml`. This plan
    STOPS at the tag push — verifying both workflows is 18-03's job. Report to the human that the
    tag is pushed and the release/deploy workflows should now be running.
  </action>
  <verify>
    <automated>git rev-parse v1.3.0 &amp;&amp; git ls-remote --tags origin v1.3.0 | grep -q 'refs/tags/v1.3.0'</automated>
  </verify>
  <done>v1.3.0 tag exists on the main merge commit and is pushed to origin; release.yml triggered.</done>
</task>

</tasks>

<verification>
Observable release-state checks (no new unit tests — see release_pipeline_note):
- Release branch pushed; PR #65 CI green on the bumped commit (human-confirmed).
- PR merged to main; main contains includes/class-slug.php and 1.3.0 version strings.
- v1.3.0 tag exists on the main merge commit and is pushed to origin.
- Every network-mutating action was explicitly authorized at a confirmation gate.
</verification>

<success_criteria>
- PR #65 merged into main with all CI checks green (ROADMAP success-criterion 2 + 3)
- v1.3.0 tag points at the merge commit and is pushed (ROADMAP success-criterion 3)
- release.yml triggered by the tag push (verified green in 18-03)
</success_criteria>

<output>
After completion, create `.planning/phases/18-release-v1.3.0/18-02-SUMMARY.md`
</output>
