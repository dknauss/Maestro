---
phase: 20-third-party-compatibility-fixes
plan: 03
type: execute
wave: 3
depends_on: ["20-02"]
files_modified:
  - assets/maestro.js
  - tests/e2e/specs/shared-slug.spec.ts
autonomous: false
requirements: [COMPAT-04]
must_haves:
  truths:
    - "In the live inline editor, a top-level item and a submenu that render the same slug are selected and edited as two independent items."
    - "Renaming/hiding the shared-slug submenu emits a qualified `parent>child` items entry on save; renaming/hiding the top-level emits the bare-slug entry — the two never overwrite each other."
    - "Submenu edit controls bind to their rendered <li> by resolved slug/href (from the model's qualified key), not by array index."
    - "A saved shared-slug config reloads with the parent and child changes each landing on the correct row."
  artifacts:
    - path: "assets/maestro.js"
      provides: "Qualified-key client model + stable submenu <li> association + qualified save payload"
      min_lines: 20
    - path: "tests/e2e/specs/shared-slug.spec.ts"
      provides: "Playwright proof of independent shared-slug parent-vs-child editing"
  key_links:
    - from: "assets/maestro.js init() model build (~L165-202)"
      to: "get_menu_model() qualified submenu key (from 20-02)"
      via: "key model[qualifiedKey] and set sli.dataset by resolved slug/href, not subLis[idx]"
      pattern: "dataset|qualifiedKey|querySelector"
    - from: "assets/maestro.js buildConfig() (~L1056-1104)"
      to: "cfg.items qualified submenu key"
      via: "emit parent>child entry instead of skipping shared-slug submenu (drop the topSlugs early-return)"
      pattern: "sub_order|items"
---

<objective>
Make COMPAT-04 usable in the live editor: the client model and save payload use
qualified `parent>child` keys, and submenu edit controls bind to their DOM <li>
by resolved slug/href instead of array index (the minimal A1b fix). A shared-slug
top-level and submenu become two independently editable items.

Purpose: The server (20-02) now targets submenus by qualified key, but the client
still keys `model[child.slug]` and zips `.wp-submenu > li` by index, and buildConfig()
explicitly SKIPS emitting a shared-slug submenu entry (maestro.js ~L1090). This plan
closes the client side so the editor can actually address both rows.
Output: A qualified-key client model, stable submenu DOM association, a qualified
save payload, and a targeted Playwright e2e proving independent editing.
</objective>

<execution_context>
@/Users/danknauss/.claude/get-shit-done/workflows/execute-plan.md
@/Users/danknauss/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/20-third-party-compatibility-fixes/20-CONTEXT.md
@.planning/phases/20-third-party-compatibility-fixes/20-02-SUMMARY.md

@assets/maestro.js
@tests/e2e/editor.spec.ts
@tests/e2e/fixtures.ts

<interfaces>
<!-- Current client seams (verified). -->

assets/maestro.js init() model build (~L165-202):
```js
D.menu.forEach(function (node) {
  model[node.slug] = { title, icon, hiddenRoles, isSub:false };
  var subLis = li.querySelectorAll('.wp-submenu > li:not(.wp-submenu-head)');
  node.submenu.forEach(function (child, idx) {
    if (!model[child.slug]) { model[child.slug] = {…, isSub:true, parent:node.slug}; } // shared-slug child SKIPPED today
    var sli = subLis[idx];               // <-- index-zip: the A1b fragility
    sli.dataset.maestroSlug = child.slug;
  });
});
```

assets/maestro.js buildConfig() (~L1056-1104):
```js
subLis.forEach(function (sli) {
  var sslug = sli.dataset.maestroSlug;
  cfg.sub_order[slug].push(sslug);
  if (topSlugs[sslug]) { return; }       // <-- shared-slug submenu override SKIPPED today
  … cfg.items[sslug] = se;
});
```

From 20-02: get_menu_model() submenu child nodes now carry a qualified `key`
(parent>child) alongside `slug`. sub_order stays bare/parent-keyed (unchanged).

Locked (20-CONTEXT.md): minimal A1b only — bind submenu controls to their <li> by a
stable attribute (resolved slug/href) for shared-slug editing. Full A1b hardening is DEFERRED.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Qualified-key client model + stable submenu DOM association</name>
  <files>assets/maestro.js</files>
  <action>
    Rework init()'s submenu handling (~L179-201) so each submenu child gets its OWN model entry
    keyed by its qualified key (node.submenu[i].key from 20-02) — remove the `if (!model[child.slug])`
    guard that suppresses a shared-slug child. Bind each child to its rendered <li> by a STABLE
    attribute, not array index: match `.wp-submenu > li:not(.wp-submenu-head)` by resolved slug /
    anchor href (store the qualified key on the <li> dataset, e.g. sli.dataset.maestroKey), instead
    of `subLis[idx]`. Update selection/popover/rename wiring that reads `model[slug]` /
    `dataset.maestroSlug` to use the qualified key for submenu rows while keeping top-level rows
    bare-keyed. In buildConfig() (~L1081-1099), emit the shared-slug submenu override under its
    qualified key (drop the `if (topSlugs[sslug]) return;` early-return for the ITEMS entry) so a
    renamed/hidden shared-slug child produces `cfg.items['parent>child']`; keep sub_order emitting
    bare child slugs (unchanged). Scope changes to what shared-slug editing needs — do not attempt
    full A1b hardening. Behavior-glue JS (DOM binding) — no node:test unit expected; e2e is the proof.
  </action>
  <verify>
    <automated>npm run test:js</automated>
  </verify>
  <done>Client model keys submenu rows by qualified key; submenu <li> bound by resolved slug/href not index; buildConfig emits qualified shared-slug items entries; existing JS unit tests still green.</done>
</task>

<task type="auto">
  <name>Task 2: Targeted Playwright e2e — independent shared-slug editing</name>
  <files>tests/e2e/specs/shared-slug.spec.ts</files>
  <action>
    Author a Playwright spec that seeds a menu with a shared-slug top/submenu pair (a CPT-shaped
    menu, e.g. a post-type whose top-level and first submenu share the slug — reuse the compat
    harness pattern or a minimal mu-plugin/fixture per tests/e2e conventions). In edit mode:
    (1) select the top-level, rename it, confirm ONLY the top-level label changes and the same-slug
    submenu keeps its label; (2) select the submenu, hide it for a role / rename it, confirm ONLY the
    submenu changes; (3) reload and confirm both changes persisted to the correct rows. Assert the
    saved payload contains a bare top-level key AND a qualified `parent>child` key (inspect the POST
    or the reloaded model). Follow the existing e2e auth/fixture setup (auth.setup.ts, fixtures.ts)
    and MAESTRO_CAPTURE gating conventions. Add the spec to the suite so the phase gate (20-06) runs it.
  </action>
  <verify>
    <automated>npm run test:e2e -- shared-slug</automated>
  </verify>
  <done>e2e proves a shared-slug parent and child are edited independently and persist to the correct rows.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Live shared-slug editing verification gate</name>
  <action>Human verifies COMPAT-04 in the running compat wp-env — a shared-slug top-level and submenu are edited independently and persist to the correct rows. See what-built/how-to-verify below.</action>
  <what-built>COMPAT-04 end-to-end in the live editor: a shared-slug top-level and submenu are now two independently selectable/editable items; changes save under distinct (bare vs qualified) keys.</what-built>
  <how-to-verify>
    1. Boot the compat wp-env (npm run compat:start) with a shared-slug plugin active (e.g. WooCommerce Products, or the seeded fixture) and enter Maestro edit mode.
    2. Rename the top-level Products item; confirm the "All Products" submenu label is unchanged.
    3. Rename/hide the "All Products" submenu; confirm the top-level Products label is unchanged.
    4. Reload; confirm both changes landed on the correct rows.
    5. Confirm no console errors and submenu selection feels correct (no mis-targeted rows).
  </how-to-verify>
  <resume-signal>Type "approved" or describe any mis-targeting / regression.</resume-signal>
</task>

</tasks>

<verification>
- npm run test:js green (no regression in existing JS units).
- npm run test:e2e -- shared-slug green.
- Manual checkpoint confirms independent shared-slug editing in the live editor.
</verification>

<success_criteria>
- SC-1 (phase): shared-slug parent vs child are independently editable in the live editor and persist correctly.
- Save payload uses bare keys for top-level and qualified `parent>child` keys for submenus.
- Minimal A1b only — no full-editor DOM-association rewrite.
</success_criteria>

<output>
After completion, create `.planning/phases/20-third-party-compatibility-fixes/20-03-SUMMARY.md`.
</output>
