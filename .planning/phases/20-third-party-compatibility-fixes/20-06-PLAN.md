---
phase: 20-third-party-compatibility-fixes
plan: 06
type: execute
wave: 5
depends_on: ["20-03", "20-05"]
files_modified:
  - assets/maestro.js
  - includes/class-assets.php
  - tests/e2e/specs/cascade-hide.spec.ts
  - TESTING.md
autonomous: false
requirements: [COMPAT-10]
must_haves:
  truths:
    - "The visibility popover shows an 'also hide children' checkbox ONLY on parent items that have children; it is always enabled."
    - "Toggling the checkbox stores the parent's cascade_hide flag via the normal full-replace save."
    - "With cascade OFF (default), hiding a parent leaves its children visible in the live menu; with cascade ON, the children are hidden too when the parent is hidden for the viewing role."
    - "Existing PHP unit, integration, JS, and Playwright e2e suites are green; WPCS clean; PHPStan clean; Plugin Check 0 errors on shippable source."
  artifacts:
    - path: "assets/maestro.js"
      provides: "Cascade checkbox in the visibility popover + payload wiring"
      contains: "cascade"
    - path: "tests/e2e/specs/cascade-hide.spec.ts"
      provides: "e2e proof of default-off vs on cascade behavior"
    - path: "TESTING.md"
      provides: "Updated canonical test counts for the phase"
  key_links:
    - from: "assets/maestro.js openVisibilityPicker() (~L908-945)"
      to: "model[slug].cascadeHide + buildConfig() cascade_hide"
      via: "checkbox shown when node has children; change toggles flag + scheduleAutosave"
      pattern: "cascade|children"
    - from: "includes/class-assets.php maestroData"
      to: "get_menu_model() cascade flag (from 20-05)"
      via: "localized model carries cascadeHide + hasChildren so the popover can gate the control"
      pattern: "cascade|submenu"
---

<objective>
Expose COMPAT-10 in the editor and close the phase with a full zero-regression
gate. Add an "also hide children" checkbox to the visibility popover (shown only
on parents that have children, always enabled), wire it to the cascade_hide flag
in the save payload, prove default-off vs on behavior with e2e, and run the full
suite + WPCS + PHPStan + Plugin Check.

Purpose: 20-05 built the server cascade + model exposure; this makes it operable
and verifies the whole phase (COMPAT-04/07/10) holds the zero-regression bar.
Output: The cascade UI, its e2e proof, and a green full-suite gate with updated
canonical counts.
</objective>

<execution_context>
@/Users/danknauss/.claude/get-shit-done/workflows/execute-plan.md
@/Users/danknauss/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/20-third-party-compatibility-fixes/20-CONTEXT.md
@.planning/phases/20-third-party-compatibility-fixes/20-05-SUMMARY.md
@.planning/phases/20-third-party-compatibility-fixes/20-03-SUMMARY.md
@TESTING.md

@assets/maestro.js
@includes/class-assets.php

<interfaces>
<!-- Visibility popover + save + localization seams (verified). -->

assets/maestro.js openVisibilityPicker() (~L908-945): builds one checkbox row per role
from D.roles, toggling model[slug].hiddenRoles and calling scheduleAutosave(). Append the
cascade checkbox here, gated on the node having children.

assets/maestro.js buildConfig() (~L1056-1104): assembles cfg.items entries via diffItem.
The cascade_hide flag is a PARENT (top-level) field — add it to the top-level entry.

includes/class-assets.php (~L95-103): localizes 'maestroData' with menu => get_menu_model().
From 20-05, get_menu_model() nodes now expose the parent cascade flag; ensure the model also
tells the client which parents have children (node.submenu.length already available).

Locked UI (20-CONTEXT.md): checkbox INSIDE the existing visibility popover; shown ONLY on
parents that have children; ALWAYS enabled (effect gated at replay, stored independently of
whether a role is currently hidden). Label wording / hint placement is executor discretion.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: "Also hide children" checkbox in the visibility popover + payload wiring</name>
  <files>assets/maestro.js, includes/class-assets.php</files>
  <action>
    In openVisibilityPicker() (~L908-945), after the role rows, append an "also hide children"
    checkbox — but ONLY when the selected node is a top-level parent WITH children (node.submenu.length
    > 0); do not render it for childless items or submenu rows. Always enabled. Initialize its checked
    state from model[slug].cascadeHide (seed model in init() from the localized cascade flag exposed by
    get_menu_model in 20-05). On change, set model[slug].cascadeHide and call scheduleAutosave().
    In buildConfig() (~L1068-1079), emit cascade_hide on the top-level items entry when set (fold it into
    the diff/entry so a cascade-only change still persists). Confirm class-assets.php localizes the
    cascade flag + children presence (get_menu_model already provides submenu arrays). Label wording is
    executor discretion; keep it accessible (real <label>, screen-reader friendly, matching the existing
    vis-row pattern). Behavior-glue JS — e2e is the proof.
  </action>
  <verify>
    <automated>npm run test:js</automated>
  </verify>
  <done>Cascade checkbox appears only on parents with children, always enabled, seeded from the model, toggling persists cascade_hide; existing JS units green.</done>
</task>

<task type="auto">
  <name>Task 2: Cascade e2e (default-off vs on)</name>
  <files>tests/e2e/specs/cascade-hide.spec.ts</files>
  <action>
    Author a Playwright spec: in edit mode, select a parent with children. (1) Hide the parent for a
    test role WITHOUT enabling cascade; view the menu as that role (or assert via reloaded model /
    replay state) and confirm the children remain visible. (2) Enable "also hide children", confirm the
    checkbox only shows on parents with children (assert it is absent on a childless item and on a
    submenu row). (3) With cascade on and parent hidden for the role, confirm the children are hidden
    for that role. (4) Confirm a cascade-hidden child page still loads by direct URL for a capable user
    (cosmetic-only, reinforcing the server guardrail from 20-05). Reuse auth.setup.ts / fixtures.ts and
    a multi-role fixture. Add to the suite for the gate below.
  </action>
  <verify>
    <automated>npm run test:e2e -- cascade-hide</automated>
  </verify>
  <done>e2e proves default-off keeps children visible, on hides them role-mirrored, checkbox gating correct, hidden child still URL-loadable.</done>
</task>

<task type="auto">
  <name>Task 3: Full zero-regression gate + canonical counts</name>
  <files>TESTING.md</files>
  <action>
    Run the complete gate for the phase (COMPAT-04/07/10): composer test:unit, composer test:integration,
    npm run test:js, npm run test:e2e (full suite — boot wp-env per project convention; use the
    alternate-port path if 8888/8889 are held, per STATE.md), composer lint (WPCS), composer
    analyse:phpstan, and Plugin Check 0 errors on shippable source (invoke with the project's
    --exclude-directories dev-tree mapping from Phase 17). Reconcile any e2e selector/assertion drift
    caused by the new cascade control DELIBERATELY (note reasoning in the commit; never silently delete).
    Update TESTING.md line 5 canonical counts (unit N/N + assertions, integration N/N + assertions) to
    the final phase totals in this commit. This is a build-then-check style gate — if Plugin Check
    surfaces only the known pre-existing dev-tree findings (see Phase 23 deferred-items.md), record them
    as pre-existing; do not fix out of scope.
  </action>
  <verify>
    <automated>composer test:unit && composer test:integration && npm run test:js</automated>
  </verify>
  <done>Full suite green (unit, integration, JS, e2e), WPCS clean, PHPStan clean, Plugin Check 0 errors on shippable source; TESTING.md counts reflect the phase.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Cascade behavior + phase zero-regression verification gate</name>
  <action>Human verifies COMPAT-10 cascade-hide behavior in the running site and confirms the phase gate summary (suites green, WPCS/PHPStan clean, Plugin Check 0 errors). See what-built/how-to-verify below.</action>
  <what-built>COMPAT-10 cascade-hide is operable in the editor and the whole phase (COMPAT-04/07/10) passes the zero-regression gate.</what-built>
  <how-to-verify>
    1. In edit mode, select a parent with children; confirm the "also hide children" checkbox appears (and is absent on a childless item / submenu row).
    2. Hide the parent for a non-admin role with cascade OFF; log in as that role (or preview) and confirm children still show.
    3. Enable cascade, confirm children disappear for that role; confirm an admin still sees the subtree (role-mirror).
    4. Confirm a cascade-hidden child page still loads by direct URL for a capable user (cosmetic-only).
    5. Confirm the gate summary: PHP unit/integration, JS, e2e green; WPCS/PHPStan clean; Plugin Check 0 errors (pre-existing dev-tree findings noted only).
  </how-to-verify>
  <resume-signal>Type "approved" or describe any regression / guardrail concern.</resume-signal>
</task>

</tasks>

<verification>
- Full suite green: composer test:unit, composer test:integration, npm run test:js, npm run test:e2e.
- composer lint (WPCS) clean; composer analyse:phpstan clean.
- Plugin Check 0 errors on shippable source (pre-existing dev-tree findings recorded, not newly introduced).
- TESTING.md canonical counts updated to final phase totals.
</verification>

<success_criteria>
- SC-3 (phase): cascade UI operable — checkbox gated to parents-with-children, default-off vs on proven.
- SC-4 (phase): existing PHP unit/integration/JS/e2e green; WPCS clean; PHPStan clean; Plugin Check 0 errors.
- Cosmetic-only guardrail reconfirmed end-to-end (hidden child still URL-loadable).
</success_criteria>

<output>
After completion, create `.planning/phases/20-third-party-compatibility-fixes/20-06-SUMMARY.md`.
</output>
