---
phase: 20-third-party-compatibility-fixes
plan: 02
type: tdd
wave: 2
depends_on: ["20-01"]
files_modified:
  - includes/class-replay.php
  - tests/integration/ReplayTest.php
  - TESTING.md
autonomous: true
requirements: [COMPAT-04]
must_haves:
  truths:
    - "A rename/hide stored under a qualified `parent>child` key applies ONLY to that submenu row, never to a top-level item that renders the same slug."
    - "A rename/hide stored under a bare top-level slug applies ONLY to the top-level item, not to a same-slug submenu — when a qualified key also exists, the qualified key wins for the submenu."
    - "A legacy bare submenu key (no qualified key present) still matches BOTH scopes exactly as today — zero behavior change until the config is re-saved."
    - "A qualified key whose parent-half matches no rendered parent is skipped and degrades silently (no misapplied override)."
    - "Both existing collision guards still fire: Axis-1 (two stored keys → one normalized key) and Axis-2 (one key → 2+ rendered slugs in a scope)."
    - "get_menu_model() and resolved_hidden_roles() resolve visibility through the SAME qualified/bare lookup replay() uses, so the editor never shows a rule replay would drop."
  artifacts:
    - path: "includes/class-replay.php"
      provides: "Qualified-key submenu resolution with legacy bare fallback; editor model in lockstep"
      contains: "get_menu_model"
    - path: "tests/integration/ReplayTest.php"
      provides: "Shared-slug fixture cases for top-vs-submenu independence + legacy fallback + parent-miss skip"
  key_links:
    - from: "Replay::replay() submenu loop"
      to: "qualified-key helper (Slug::normalize_qualified) from 20-01"
      via: "build parent>child candidate per rendered child, match before bare fallback"
      pattern: "normalize_qualified|>"
    - from: "Replay::get_menu_model() submenu nodes"
      to: "qualified key string"
      via: "each child node carries its parent>child key so the client + save agree"
      pattern: "submenu"
---

<objective>
Wire COMPAT-04 level-qualified resolution into the replay engine and the editor
model. A qualified `parent>child` override resolves to exactly one submenu row;
a bare top-level key resolves to exactly the top-level; legacy bare submenu keys
keep matching both scopes until re-saved. The editor model resolves through the
same lookup so display and apply never drift.

Purpose: Close the shared-slug collision (WooCommerce Products top + All Products
submenu, Yoast, LifterLMS CPTs) where a single bare key lands on both — the
COMPAT-04 root cause in the replay seam.
Output: A replay() submenu loop that prefers qualified keys with a legacy bare
fallback, both collision guards intact, and a get_menu_model() that emits
qualified submenu identity.
</objective>

<execution_context>
@/Users/danknauss/.claude/get-shit-done/workflows/execute-plan.md
@/Users/danknauss/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/20-third-party-compatibility-fixes/20-CONTEXT.md
@.planning/phases/20-third-party-compatibility-fixes/20-01-SUMMARY.md
@.planning/compat/BACKLOG.md

@includes/class-replay.php
@tests/integration/ReplayTest.php

<interfaces>
<!-- Current replay seams (verified line numbers). -->

From includes/class-replay.php:
```php
public function replay(): void;                       // top loop ~L119-153; submenu loop ~L156-264
// title write: $menu[pos][0] ~L134 (top); $submenu[parent][pos][0] ~L188 (sub)
// hide: unset() ~L149-151 (top), ~L190-192 (sub); via is_hidden_for_current_user() ~L301
private function normalized_items( array $items, $base ): array;   // ~L364 → [norm_items, norm_skip]  (Axis-1 guard)
private function resolved_hidden_roles( $slug, $norm_items, $norm_skip, $base ): array; // ~L393
public function get_menu_model(): array;              // ~L411 → [{slug,liId,title,icon,hiddenRoles,submenu:[{slug,title,hiddenRoles}]}]
```

Axis-2 guards are the pre-scans in each loop (top ~L103-117, sub ~L158-174).
COMPAT-04 root cause: both loops look up the SAME flat $norm_items[$nk] by bare
child slug, so a bare key matches in top AND submenu scope.

From 20-01 (this plan's dependency):
```php
Slug::is_qualified($key): bool;
Slug::normalize_qualified($key, $base): string;  // each half normalized independently, '' if a half is empty
```

Locked resolution rules (20-CONTEXT.md):
- New model MUST make submenu overrides addressable by qualified key.
- Qualified key wins for a submenu row; if none, fall back to the bare normalized
  child key (legacy — still matches both scopes → today's behavior, zero regression).
- Parent-half miss (qualified key's parent doesn't match the rendered parent) → skip, degrade silently.
- Keep BOTH Axis-1 and Axis-2 guards — level-qualification does not replace them.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Qualified-key submenu resolution in replay() with legacy bare fallback</name>
  <files>includes/class-replay.php, tests/integration/ReplayTest.php</files>
  <behavior>
    - Shared-slug pair (top-level `edit.php?post_type=product` + submenu `edit.php?post_type=product` under that parent): a qualified override `edit.php?post_type=product>edit.php?post_type=product` renames/hides ONLY the submenu; the top-level is untouched.
    - A bare top-level override renames/hides ONLY the top-level; the same-slug submenu is untouched when a qualified key is present.
    - Legacy bare submenu key with NO qualified key present: still applies to both the top-level and the same-slug submenu (today's behavior) — assert against a pre-existing-style config.
    - Qualified key whose parent-half matches no rendered parent: override is skipped; nothing else changes.
    - Axis-1: two stored keys normalizing to one → apply nothing (extend the existing guard to qualified keys). Axis-2: one key matching 2+ distinct rendered slugs in a scope → skip (unchanged).
    - Normalization applies per half: a host-moved/ver=/utm_/&amp; twin of a qualified key still resolves.
  </behavior>
  <action>
    In the submenu loop (~L156-264): for each rendered child under $parent, build a candidate
    qualified normalized key from (rendered parent slug > rendered child slug) via
    Slug::normalize_qualified, and resolve against a qualified-aware lookup FIRST. If no qualified
    override exists for this parent>child, fall back to the existing bare normalized child key
    (legacy path — leave that code path's both-scope matching intact). Extend normalized_items()
    (or add a parallel qualified lookup) so Axis-1 covers qualified keys too; keep the per-parent
    Axis-2 pre-scan. For a stored qualified key, if its parent-half matches no rendered parent in
    this pass, do not apply it (silent skip). Do NOT change the top-level loop's bare-key behavior.
    Icons remain top-level only (submenu keys never carry one — enforced in 20-01).
    TDD: add failing ReplayTest cases first (seed a shared-slug menu/submenu; RED in working tree),
    then implement to green. ONE green commit (test+impl). Update TESTING.md integration
    count/assertions in the SAME commit.
  </action>
  <verify>
    <automated>composer test:integration -- --filter ReplayTest</automated>
  </verify>
  <done>Shared-slug top-vs-submenu independence proven; legacy bare fallback still both-scope; parent-miss skip; both guards intact; integration green; TESTING.md updated.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Editor model + resolved_hidden_roles emit/consume qualified keys</name>
  <files>includes/class-replay.php, tests/integration/ReplayTest.php, TESTING.md</files>
  <behavior>
    - get_menu_model() submenu child nodes carry their qualified `parent>child` key (in addition to raw slug) so the client and the next full-replace save address the submenu independently of a same-slug top-level.
    - A submenu child's hiddenRoles in the model reflect a qualified override when present, and fall back to a legacy bare override otherwise — matching exactly what replay() would apply (no drift).
    - A top-level node's hiddenRoles reflect the bare top-level override only, never a same-slug submenu's qualified override.
    - Round-trip: save a qualified-key config, rebuild the model, and the submenu node shows the qualified rule while the top-level node does not.
  </behavior>
  <action>
    Update get_menu_model() (~L411) so each submenu node exposes its qualified key
    (e.g. add a `key` / `qualifiedKey` field alongside `slug`). Update resolved_hidden_roles()
    (~L393) — or add a qualified-aware sibling — so the model resolves submenu visibility through
    the qualified-first/bare-fallback lookup used in Task 1, keeping editor display and replay apply
    in lockstep (the standing "get_menu_model and replay must agree" contract). Keep the top-level
    node resolution bare-key only. TDD: failing ReplayTest model cases first (RED in working tree),
    implement to green, ONE green commit. Update TESTING.md integration count/assertions in the
    SAME commit.
  </action>
  <verify>
    <automated>composer test:integration -- --filter ReplayTest</automated>
  </verify>
  <done>Editor model emits qualified submenu identity and resolves visibility in lockstep with replay; round-trip proven; integration green; TESTING.md updated.</done>
</task>

</tasks>

<verification>
- composer test:integration green (ReplayTest, incl. new shared-slug + model cases).
- composer test:unit still green (no regression from the config foundation).
- composer lint + analyse:phpstan clean on includes/class-replay.php.
- No maestro.js change in this plan (client A1b is 20-03).
</verification>

<success_criteria>
- SC-1 (phase): a rename/hide on a top-level slug does not hit a same-slug submenu, and vice versa — proven against shared-slug fixtures.
- Legacy bare-key configs behave exactly as today until re-saved.
- Editor model and replay agree on qualified resolution.
- TESTING.md counts updated.
</success_criteria>

<output>
After completion, create `.planning/phases/20-third-party-compatibility-fixes/20-02-SUMMARY.md`.
</output>
