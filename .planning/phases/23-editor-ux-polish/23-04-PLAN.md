---
phase: 23-editor-ux-polish
plan: 04
type: execute
wave: 4
depends_on: [03]
files_modified:
  - assets/maestro.css
  - assets/maestro.js
  - includes/class-assets.php   # only if the wp-pointer look is enqueued (adds the style dep); see Task 1
autonomous: false
executor_model: opus   # carries the wp-pointer-adaptation live-iteration judgment call (CONTEXT §Process hybrid, §Executor-model guidance: opus for wp-pointer adaptation + final visual calls); the BUG-08 centering + in-menu token pass are mechanical
requirements: [BUG-08, UX-13]
must_haves:
  truths:
    - "The first-run coachmark's text and button are vertically centered within its box — no visual off-center (BUG-08 fixed)"
    - "The coachmark adopts the wp-pointer look (core's admin-tour idiom): arrow/pointer, card, and controls read as a core pointer, not a bespoke tooltip"
    - "The coachmark's BEHAVIOR, 5 steps, copy, focus trap, and step logic are UNCHANGED — restyle only"
    - "In-menu selection keeps the current tinted-row + inset-accent-bar treatment, token-aligned to core blue"
    - "The in-menu modified badge switches from its own idiom to the non-colour dot idiom introduced in plan 01"
    - "No storage/REST/behavioral change: tour steps, selection, and modified-state semantics are untouched"
  artifacts:
    - path: "assets/maestro.css"
      provides: "BUG-08 vertical centering of the coachmark; wp-pointer-look restyle of .maestro-tour; token-aligned selection; dot-idiom modified badge"
      contains: "maestro-tour"
    - path: "assets/maestro.js"
      provides: "Any DOM/class adjustments the wp-pointer look + dot badge need (behavior/steps/copy frozen)"
      contains: "maestro-tour"
  key_links:
    - from: "assets/maestro.css .maestro-tour"
      to: "WP core wp-pointer tokens"
      via: "card/arrow/controls styled to the core pointer look (enqueued-and-reused OR replicated locally — decided in-plan)"
      pattern: "maestro-tour"
    - from: "assets/maestro.css .maestro-modified-badge"
      to: "plan 01 non-colour dot idiom"
      via: "the modified marker uses the same dot glyph/placement chosen in plan 01"
      pattern: "maestro-modified"
---

<objective>
Finish the surface conversion: fix BUG-08 (vertically center the first-run coachmark's
text + button), restyle the coachmark to the wp-pointer look (restyle ONLY — behavior,
5 steps, copy, focus trap FROZEN), and token-align the in-menu selection + modified
badge — keeping the tinted-row + inset-accent-bar selection (already core blue) and
switching the modified badge to the non-colour dot idiom from plan 01.

Purpose: the coachmark and the in-menu selection/badges are the last edit-mode surfaces
not yet on core idioms. The coachmark IS the first-run experience (UX-11 replaced the
old first-run pulse/cue), so BUG-08's "first-run banner" centering lands here alongside
the wp-pointer restyle. After this plan, every surface named in UX-13 is converted.

This plan carries the wp-pointer-adaptation live-iteration judgment call (opus, per
CONTEXT §Process) — settled against a running site, outcome recorded in the SUMMARY.

Output: centered coachmark (BUG-08), wp-pointer-look coachmark restyle, token-aligned
selection, dot-idiom modified badge, and a recorded wp-pointer-adaptation outcome
(including the enqueue-vs-replicate decision).
</objective>

<execution_context>
@/Users/danknauss/.claude/get-shit-done/workflows/execute-plan.md
@/Users/danknauss/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-editor-ux-polish/23-CONTEXT.md

# Depends on plan 01 (the non-colour dot idiom the modified badge must adopt) and
# plan 03 (shared maestro.css). Read both SUMMARYs for the exact dot glyph/placement
# and the popover token decisions the coachmark card should echo.
@.planning/phases/23-editor-ux-polish/23-01-SUMMARY.md
@.planning/phases/23-editor-ux-polish/23-03-SUMMARY.md

# Surfaces (verify line numbers before editing; file drifts after 01/02/03)
@assets/maestro.css
@assets/maestro.js

<interfaces>
<!-- Key surfaces (assets/maestro.css + maestro.js, post-01/02/03 — verify lines). -->

Coachmark (the first-run experience; UX-11, behavior FROZEN):
- CSS .maestro-tour (~:516) + .maestro-tour-progress / -text / -controls / -skip / -back
  (~:531–559): a white card (border #c3c4c7, radius 4px, shadow, z 100001, width 280px)
  with a progress eyebrow, body text, and a controls row (Skip / Back / Next). NO
  entrance animation (reduced-motion-safe by design). BUG-08: the box currently has no
  vertical centering of its content — center the text + controls within the box.
- JS tour DOM (assets/maestro.js): box built in a render fn (~L1367): `el('div',
  'maestro-tour')` -> .maestro-tour-progress, .maestro-tour-text, .maestro-tour-controls
  (Skip .button-link .maestro-tour-skip, Back .button .maestro-tour-back, Next .button
  .button-primary .maestro-tour-next). tourSteps() (~L1263) = the 5 FROZEN steps;
  startTour/endTour/onTourKeydown/onTourFocusIn = the FROZEN focus-trap + step logic.
  Do NOT change steps, copy, placement anchors, or trap logic — restyle/DOM-class only.
  The "?" replay entry is .maestro-tour-help (~L578) in the toolbar.
- wp-pointer reference: core's admin-tour component ships .wp-pointer / .wp-pointer-content
  / .wp-pointer-arrow / .wp-pointer-buttons in wp-admin. The coachmark should MIMIC that
  look (card + directional arrow + core buttons). DECIDE in-plan (CONTEXT §Claude's
  Discretion): enqueue core's `wp-pointer` stylesheet and reuse its classes, OR replicate
  the look locally in maestro.css — weigh payload budget (enqueue = one more stylesheet
  dependency) vs drift risk (replicate = our copy can drift from core). Justify in the
  plan/SUMMARY.

In-menu selection (KEEP treatment, token-align):
- .maestro-editing #adminmenu .maestro-selected > a (~:195): tinted row
  background rgba(34,113,177,.22) + inset accent bar box-shadow inset 3px 0 0 #2271b1
  (already core blue). Keep the treatment; confirm the tint + bar use core #2271b1 and
  align if drifted. Focus ring at ~:201 (2px #2271b1) stays.

In-menu modified badge (switch to plan 01's dot idiom):
- .maestro-editing #adminmenu .maestro-modified > a (~:95) + .maestro-modified-badge
  (~:98): the current in-menu modified marker. Switch it to the SAME non-colour dot
  glyph/placement plan 01 chose for the toolbar/row modified marker (read 23-01-SUMMARY
  for the exact glyph). Keep the screen-reader .maestro-modified-sr "(modified)" text
  (WCAG 1.4.1). e2e targets .maestro-modified / .maestro-modified-badge /
  .maestro-modified-sr — keep those hooks; any assertion change lands in plan 23-05.

Constraint: coachmark BEHAVIOR/STEPS/COPY frozen; selection/modified SEMANTICS frozen.
el() stays textContent-based (XSS-safe). Keep the mode icon a real aria-hidden DOM
child (BUG-04) — not touched here but do not regress. If any i18n string changes,
class-assets.php + the payload-budget integration test move in one commit (they don't
change here unless the wp-pointer restyle needs it — avoid).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: BUG-08 centering + coachmark wp-pointer restyle + in-menu selection/badge tokens</name>
  <files>assets/maestro.css, assets/maestro.js</files>
  <action>
    BUG-08 (assets/maestro.css .maestro-tour ~:516): vertically center the coachmark's
    text + controls within the box — the reported off-center. Add the layout that
    balances the progress eyebrow / body / controls row (e.g. flex column with centered
    cross-axis where appropriate, or corrected padding) so nothing sits visually
    off-center. Confirmed by before/after screenshot in plan 23-05.

    Coachmark wp-pointer restyle (RESTYLE ONLY — steps/copy/trap FROZEN):
    - Adopt the wp-pointer look for .maestro-tour + its progress/text/controls: core
      pointer card + a directional arrow + core-styled buttons. FIRST decide (and state
      in the plan/SUMMARY) whether to (a) enqueue core's `wp-pointer` stylesheet in
      includes/class-assets.php and reuse .wp-pointer* classes, or (b) replicate the
      look locally in maestro.css. Weigh payload budget vs drift risk (CONTEXT §Claude's
      Discretion). If enqueuing, add `wp-pointer` to the style deps (class-assets.php)
      and adapt the tour DOM classes in maestro.js to the pointer structure — WITHOUT
      changing steps/copy/anchors/trap. If replicating, keep our classes and match the
      core pointer tokens in maestro.css.
    - Keep NO entrance animation (reduced-motion-safe). Keep the focus-visible ring
      (2px #2271b1). Colour reserved for errors/destructive — the pointer is neutral.

    In-menu selection (KEEP + token-align): confirm .maestro-selected tint
    (rgba(34,113,177,.22)) + inset accent bar (#2271b1) are core blue; align if drifted.
    Keep the :focus-visible ring.

    In-menu modified badge -> dot idiom: switch .maestro-modified-badge to the SAME
    non-colour dot glyph/placement plan 01 chose (read 23-01-SUMMARY). Keep the SR
    "(modified)" text. If the JS that appends the badge (buildToolbar/selection wiring
    in maestro.js, the .maestro-modified-badge / .maestro-modified-sr append) needs a
    class/DOM tweak to match plan 01's dot, make it — but keep the e2e hooks
    (.maestro-modified, .maestro-modified-badge, .maestro-modified-sr) intact; assertion
    updates land in plan 23-05, not here.

    No behavioral change: tour steps/copy/trap, selection, and modified semantics frozen.
  </action>
  <verify>
    <automated>grep -nE "maestro-tour|maestro-modified|maestro-selected" assets/maestro.css | head</automated>
    <automated>! grep -nE "#6bc187|#f0b849|#dba617" assets/maestro.css</automated>
  </verify>
  <done>Coachmark text + button are vertically centered (BUG-08); .maestro-tour reads as a wp-pointer (card + arrow + core buttons) with the enqueue-vs-replicate decision made and applied; selection keeps the tinted-row + inset #2271b1 bar; the in-menu modified badge uses plan 01's non-colour dot with SR text preserved; steps/copy/trap/selection/modified behavior unchanged; no removed-legacy colour present.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Live-iteration — wp-pointer adaptation + final coachmark read (opus)</name>
  <what-built>
    The first-run coachmark restyled to the wp-pointer look, with BUG-08 centering, and
    the in-menu selection/modified-dot token pass. This checkpoint settles the
    wp-pointer-adaptation JUDGMENT call CONTEXT §Process reserves for live iteration
    against a running site (opus, per §Executor-model guidance): does our coachmark read
    as a native core pointer, and is the enqueue-vs-replicate choice the right one?
  </what-built>
  <files>assets/maestro.css, assets/maestro.js, includes/class-assets.php</files>
  <action>
    Iterate live against the running site: tune the .maestro-tour pointer tokens/arrow
    in maestro.css (and, if enqueuing core wp-pointer, the deps in class-assets.php +
    the tour DOM classes in maestro.js) until the coachmark reads as a native core
    pointer, without touching steps/copy/anchors/trap. Confirm the enqueue-vs-replicate
    decision holds, then present the running result for the human-verify steps below
    and record the decision + final read in the SUMMARY.
  </action>
  <how-to-verify>
    Bring up wp-env / Studio, authenticated, enter the menu editor. Trigger the
    first-run tour (clear localStorage `maestroFirstRunDone`, or click the toolbar "?"
    to replay):
    1. BUG-08: confirm the text + Next/Back/Skip controls are vertically centered in the
       box across all 5 steps — no off-center.
    2. wp-pointer read: does the card + arrow + buttons look like a core admin pointer
       (compare against a real core pointer if one is available)? Adjust the pointer
       tokens / arrow until it reads native. Confirm the enqueue-vs-replicate decision
       holds up (no payload bloat if enqueued; no visible drift if replicated).
    3. Confirm behavior is UNCHANGED: all 5 steps, copy, Back/Next/Skip, focus trap, and
       the anchor placement still work exactly as shipped.
    4. In-menu: select an item, modify it, and confirm the selection tint + inset accent
       bar and the non-colour modified DOT (matching plan 01) read correctly; confirm the
       SR "(modified)" text is present.
    5. Spot-check Default + Modern + Midnight admin colour schemes.
    Record the enqueue-vs-replicate decision + rationale and the final pointer-adaptation
    read (and any per-scheme note) in the SUMMARY.
  </how-to-verify>
  <verify>
    <manual>Live visual check of the first-run tour (all 5 steps) + in-menu selection/dot on Default + Modern + Midnight (steps below).</manual>
  </verify>
  <done>The coachmark reads as a native wp-pointer with BUG-08 centering confirmed and behavior verified unchanged; the enqueue-vs-replicate decision and the per-scheme read are applied and recorded in the SUMMARY.</done>
  <resume-signal>Type "approved" once the coachmark reads as a native wp-pointer, BUG-08 centering is confirmed, behavior is verified unchanged, and the decision is recorded — or describe adjustments needed.</resume-signal>
</task>

</tasks>

<verification>
Maps to ROADMAP success criterion 3 (coachmark + in-menu selection/badges adopt core
idioms, spot-checked on Default + Modern + Midnight — before/after screenshots in plan
23-05), criterion 4 (first-run banner text + button vertically centered — before/after
screenshot in 23-05), and criterion 5 (suites stay green; e2e assertions on
.maestro-tour / .maestro-modified* updated deliberately in plan 23-05, not silently here).

TDD boundary (CONTEXT §Methodology): pure styling / frozen-behavior restyle — NO unit
TDD (no changed pure logic). Covered by Playwright (tour.spec.ts behavior stays green)
+ before/after screenshots in plan 23-05 on the three colour schemes.

- `grep` confirms the removed-legacy colours are absent across the coachmark/selection.
- tour.spec.ts (behavior/steps/trap) must remain green unchanged — verified at the plan
  23-05 gate; behavior is frozen here.
- wp-pointer enqueue-vs-replicate decision + BUG-08 + colour-scheme notes recorded in
  the SUMMARY (Task 2).
</verification>

<success_criteria>
- BUG-08: coachmark text + button vertically centered (confirmed by screenshot in 23-05).
- Coachmark reads as a native wp-pointer; enqueue-vs-replicate decided and applied; behavior/steps/copy/trap frozen.
- In-menu selection keeps the tinted-row + inset #2271b1 accent bar (token-aligned).
- In-menu modified badge uses plan 01's non-colour dot idiom; SR "(modified)" text preserved.
- No storage/REST/behavioral change; no removed-legacy colour present.
</success_criteria>

<output>
After completion, create `.planning/phases/23-editor-ux-polish/23-04-SUMMARY.md`.
Record: the wp-pointer enqueue-vs-replicate decision and its payload-budget-vs-drift
rationale; how BUG-08 centering was fixed; confirmation that tour behavior/steps/copy/
trap are unchanged; the final pointer read + any admin-colour-scheme note (Modern/Midnight).
</output>
