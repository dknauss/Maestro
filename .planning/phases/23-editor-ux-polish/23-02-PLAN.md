---
phase: 23-editor-ux-polish
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - assets/maestro.js
  - assets/maestro.css
  - assets/maestro-logic.js
  - tests/js/mode-zone-placement.test.mjs
  - includes/class-assets.php
autonomous: true
executor_model: sonnet   # REVISED 2026-07-05: the pinned-zone live-iteration judgment was settled by the user (scrap it). Remaining work is mechanical removal + a toolbar relabel/highlight + a CSS underline-bug fix — sonnet. Orchestrator screenshot-verifies the toolbar read after.
requirements: [UX-09, UX-13]
must_haves:
  truths:
    - "The pinned menu-column zone is GONE: no mode/status zone in #adminmenuwrap, no modeZonePlacement seam, no 782px relocation gate, no #collapse-menu hide/restore manipulation anywhere"
    - "The pencil 'Edit Mode' chip is removed from the toolbar entirely"
    - "The edit-mode indicator is the bottom-toolbar Exit control, relabelled 'Exit Menu Editor', carrying a subtle neutral (menu-native, non-colour) background highlight so its presence names the mode"
    - "The muted save-status indicator (spinner / 'Saved' / 'Save failed') remains in the bottom toolbar and still updates correctly (setStatus + modeStatusLabel unchanged from plan 01)"
    - "The Reset All control's underline bug is fixed — no stray/inconsistent underline on its icon or link text"
    - "Colour stays reserved for errors + destructive Reset All only; the Exit highlight is neutral"
    - "npm run test:js is green (the obsolete mode-zone-placement.test.mjs is deleted, not left failing)"
  artifacts:
    - path: "assets/maestro.js"
      provides: "Toolbar builds Exit as 'Exit Menu Editor' (highlighted); the mode-chip build + relocateModeZone + matchMedia wiring + .maestro-mode-zone wrapper are removed; save-status appended to the toolbar"
      contains: "maestro-exit"
    - path: "assets/maestro.css"
      provides: "Subtle neutral background highlight on .maestro-exit; Reset All underline fix; removal of the pinned-zone / .maestro-mode-zone / #collapse-menu-hide rules"
      contains: "maestro-exit"
    - path: "assets/maestro-logic.js"
      provides: "modeZonePlacement seam removed from the module + its api export (modeStatusLabel and the other seams stay)"
      contains: "modeStatusLabel"
    - path: "includes/class-assets.php"
      provides: "The Exit i18n string is 'Exit Menu Editor' (within the localized-payload budget)"
      contains: "Exit Menu Editor"
  key_links:
    - from: "assets/maestro.js buildToolbar"
      to: "assets/maestro.css .maestro-exit highlight"
      via: "the exit control's class + i18n label"
      pattern: "maestro-exit"
    - from: "assets/maestro.js setStatus"
      to: "assets/maestro-logic.js modeStatusLabel"
      via: "window.maestroLogic.modeStatusLabel (unchanged from plan 01)"
      pattern: "modeStatusLabel"
---

<objective>
REVISED 2026-07-05 (user live-iteration decision — see 23-CONTEXT.md §UX-09 geometry).
The original UX-09 "pin the mode/status zone to the admin-menu column" approach was
built, viewed on the running site, and SCRAPPED: it read out-of-sync/misaligned and
was not viable at the menu-column bottom (a lonely pencil in a near-empty 160×45 slot
below the fold).

New approach: the edit-mode signal collapses into the bottom toolbar's Exit control.
Relabel Exit to "Exit Menu Editor" and give it a subtle neutral (menu-native,
non-colour) background highlight so its presence names the mode — the wp-native
Customizer/Site-Editor "Exit" idiom. Remove the pencil "Edit Mode" chip. The muted
save-status stays in the toolbar (plan 01). Also fix a reported underline bug on the
Reset All control.

Purpose: deliver UX-09's intent (a clear, native edit-mode indicator) without the
fragile menu-column relocation machinery, and clear a visible Reset All defect.

Output: the pinned-zone implementation removed cleanly; a relabelled, highlighted
"Exit Menu Editor" control; the Reset All underline fixed; node:test green.
</objective>

<execution_context>
@/Users/danknauss/.claude/get-shit-done/workflows/execute-plan.md
@/Users/danknauss/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-editor-ux-polish/23-CONTEXT.md
@.planning/phases/23-editor-ux-polish/23-01-SUMMARY.md

# Surfaces (verify line numbers before editing)
@assets/maestro.js
@assets/maestro.css
@assets/maestro-logic.js
@includes/class-assets.php

<current_state>
IMPORTANT — the working tree currently CONTAINS the scrapped pinned-zone
implementation (committed as `d768801` + `537b2f8`, still on this branch). This plan
does NOT start from a clean toolbar — it REMOVES that implementation and replaces it
with the Exit-control approach, committed forward (no git revert/reset; the history
honestly shows "tried the zone → replaced it").

What `d768801` + `537b2f8` added and that this plan must UNWIND:
- `assets/maestro-logic.js`: the `modeZonePlacement( viewportWidth )` pure function +
  its `api` export. DELETE it (keep modeStatusLabel + the other seams).
- `tests/js/mode-zone-placement.test.mjs`: the node:test for that seam. DELETE the file.
- `assets/maestro.js`: `relocateModeZone()`, the `matchMedia('(max-width:782px)')`
  listener + its call sites, the `.maestro-mode-zone` wrapper around modeEl+statusEl,
  and the `#collapse-menu` hide/restore (the `.maestro-collapse-hidden` add/remove).
  REMOVE all of it. The save-status element (`statusEl`) must remain, appended into the
  bottom toolbar (as in plan 01, before the wrapper was introduced). Restore the
  pre-existing editing-wide `#collapse-menu` neutralisation if `537b2f8` changed it
  (537b2f8 replaced an unconditional collapse-hide with the class-gated one — revert to
  whatever plan-01/pre-02 behaviour was; the collapse control is simply left alone now).
- `assets/maestro.css`: the `.maestro-mode-zone` layout, the menu-column docked rules,
  the `.maestro-editing #collapse-menu` hide, and the `@media (max-width:782px)` rejoin
  additions for the zone. REMOVE them.
</current_state>

<interfaces>
Toolbar DOM (assets/maestro.js, buildToolbar ~L426–600):
- The pencil mode chip is `modeEl` (`.maestro-mode-label` with an aria-hidden
  dashicons-edit child + a `.maestro-btn-label` "Edit Mode"). REMOVE the whole modeEl
  build — the mode is now named by the Exit control. (Do not leave an orphaned
  aria-label referencing it.)
- The Exit control is `.maestro-exit` (`var exit = el('a','button maestro-exit')`
  ~L591, onExit handler ~L594). Its visible label text comes from the i18n `exit`
  string. Change the label to "Exit Menu Editor" (via the i18n string, below) and add
  the highlight class/style. Keep onExit behaviour (full-navigation exit) unchanged.
- Save-status `statusEl` (`.maestro-status[role=status][aria-live=polite]` +
  `.maestro-status-text.screen-reader-text`) stays; append it into the toolbar. setStatus
  (~L1101) + modeStatusLabel are unchanged from plan 01. el() stays textContent-based.

i18n (includes/class-assets.php `i18n` array ~L99–160):
- There is an `exit`/exit-label string and an `exitUrl` (`remove_query_arg('maestro_edit')`).
  Change the exit LABEL to "Exit Menu Editor". Keep every new/changed string within the
  localized-payload budget asserted by the PHP integration tests (grep the test that
  asserts the budget; if "Exit Menu Editor" pushes it over, note it — but a few chars
  should be fine). Do NOT touch exitUrl.

Reset All underline bug (assets/maestro.css, the `.maestro-reset-all` / `.button-link-delete`
rules from plan 01, ~:160s):
- Symptom (reported from the running site): the Reset All control shows a stray/
  inconsistent underline on its icon and/or link text. Diagnose on http://localhost:8888
  (?maestro_edit=1) — likely the `<a>` default text-decoration or a core `.button-link`/
  link rule leaking underline onto the icon+label. Fix so Reset All reads as a clean
  destructive text link (underline on hover only, or none — match core `.button-link-delete`,
  which is not underlined by default; the dashicon must never be underlined). Keep it red
  (destructive) — colour is allowed here.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove the scrapped pinned-zone implementation; restore save-status to the toolbar</name>
  <files>assets/maestro-logic.js, tests/js/mode-zone-placement.test.mjs, assets/maestro.js, assets/maestro.css</files>
  <action>
    Unwind everything the pinned-zone attempt added (see <current_state>):
    - Delete `modeZonePlacement` from assets/maestro-logic.js and remove it from the `api`
      export. Keep modeStatusLabel + the other seams intact.
    - Delete tests/js/mode-zone-placement.test.mjs.
    - In assets/maestro.js: remove `relocateModeZone()`, the matchMedia(782) listener and
      all its call sites, the `.maestro-mode-zone` wrapper, and the `#collapse-menu`
      hide/restore (`.maestro-collapse-hidden`). Append the save-status `statusEl` directly
      into the bottom toolbar as it was in plan 01. Leave #collapse-menu alone (no plugin
      manipulation of it at all).
    - In assets/maestro.css: remove the `.maestro-mode-zone` layout, the menu-column docked
      rules, the `.maestro-editing #collapse-menu` hide, and the zone-specific
      `@media (max-width:782px)` additions.
    - Remove the pencil mode chip (`modeEl` / `.maestro-mode-label`) build in buildToolbar
      and any now-dead `.maestro-mode-label` CSS.
    Run `npm run test:js` — must be green with mode-zone-placement.test.mjs gone (back to
    the plan-01 suite count). setStatus must still work (save-status renders in the toolbar).
  </action>
  <verify>
    <automated>npm run test:js</automated>
    <automated>! grep -rnE "modeZonePlacement|maestro-mode-zone|maestro-collapse-hidden|max-width:782px" assets/ && echo "zone machinery gone ✓"</automated>
    <automated>test ! -f tests/js/mode-zone-placement.test.mjs && echo "obsolete test deleted ✓"</automated>
  </verify>
  <done>No modeZonePlacement / .maestro-mode-zone / #collapse-menu manipulation / 782px zone rules remain anywhere; the pencil mode chip is gone; save-status renders in the toolbar and updates via setStatus; node:test green.</done>
</task>

<task type="auto">
  <name>Task 2: Relabel Exit → "Exit Menu Editor" + subtle neutral highlight; fix Reset All underline</name>
  <files>assets/maestro.js, assets/maestro.css, includes/class-assets.php</files>
  <action>
    - i18n: change the Exit label string in includes/class-assets.php to "Exit Menu Editor"
      (keep within the localized-payload budget asserted by the PHP integration tests; grep
      that test and confirm). Do not touch exitUrl.
    - Exit control (assets/maestro.js buildToolbar + assets/maestro.css .maestro-exit): the
      control now names the mode. Add a subtle neutral (menu-native, non-colour) background
      highlight so its presence reads as the active-mode affordance — e.g. a low-contrast
      lightened background (rgba white ~0.10–0.14 on the dark toolbar) + the existing
      exit-door dashicon + "Exit Menu Editor" text. Keep core-blue focus ring. NO colour
      (reserve red for error/destructive). Keep onExit behaviour unchanged.
    - Reset All underline bug (assets/maestro.css): diagnose on the running site
      (http://localhost:8888, ?maestro_edit=1) and fix the stray/inconsistent underline on
      the Reset All icon + link text. Reset All must read as a clean destructive text link
      (core `.button-link-delete` idiom — dashicon never underlined; text underline on hover
      only, or none, matching core). Stays red.
    - No behavioural change to save/reorder/hide/icon/rename/exit. el() stays textContent-based.
  </action>
  <verify>
    <automated>grep -n "Exit Menu Editor" includes/class-assets.php</automated>
    <automated>grep -nE "maestro-exit" assets/maestro.css</automated>
    <automated>npm run test:js</automated>
  </verify>
  <done>The Exit control reads "Exit Menu Editor" with a subtle neutral background highlight and names the edit mode; the Reset All underline bug is fixed (clean destructive text link, icon never underlined); i18n stays within payload budget; node:test green; no behavioural change.</done>
</task>

</tasks>

<verification>
Maps to ROADMAP success criterion 1 (REVISED): the edit-mode indicator lives in the
bottom toolbar — "Exit Menu Editor" names the mode with a subtle neutral highlight, muted
save-status alongside, no menu-column zone, no 782px relocation — confirmed by before/after
screenshot in plan 23-05. Also maps to criterion 2 (colour reserved; the Exit highlight is
neutral; Reset All stays the red destructive text link — now without the underline defect)
and criterion 5 (suites green; e2e assertions that referenced the mode chip / relocated zone
/ modeZonePlacement are updated deliberately in plan 23-05).

TDD boundary (CONTEXT §Methodology, revised): no changed pure logic in this plan — the
modeZonePlacement seam is DELETED, and modeStatusLabel (plan 01) is unchanged. All work here
is removal + styling + one i18n string. Covered by Playwright + before/after screenshots in
plan 23-05; no invented unit tests.

- `npm run test:js` green (mode-zone-placement.test.mjs deleted; no failing suite).
- grep confirms the zone machinery is gone and "Exit Menu Editor" is present.
- Orchestrator screenshot-verifies the toolbar read (Exit highlight + Reset All underline fix)
  on the running site before advancing to Wave 3.
</verification>

<success_criteria>
- The pinned menu-column zone, modeZonePlacement seam (+ its test), 782px gate, and #collapse-menu manipulation are all removed.
- The pencil "Edit Mode" chip is gone; the mode is named by the toolbar "Exit Menu Editor" control.
- "Exit Menu Editor" carries a subtle neutral (non-colour) background highlight; onExit behaviour unchanged.
- Muted save-status stays in the toolbar and updates via setStatus.
- Reset All underline bug fixed (clean destructive text link; icon never underlined; stays red).
- npm run test:js green; i18n within payload budget; no behavioural change.
</success_criteria>

<output>
After completion, create `.planning/phases/23-editor-ux-polish/23-02-SUMMARY.md`.
Record: what was removed from the scrapped pinned-zone attempt (files/functions/CSS); the
Exit relabel + the exact highlight treatment chosen (colour/opacity) and why it reads neutral;
the Reset All underline root cause + the fix; the i18n payload-budget check for "Exit Menu
Editor"; and confirmation setStatus still drives the toolbar save-status.
</output>
