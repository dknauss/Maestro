---
phase: 23-editor-ux-polish
plan: 03
type: execute
wave: 3
depends_on: [02]
files_modified:
  - assets/maestro.css
autonomous: true
executor_model: sonnet   # mechanical token-alignment pass over already-near-core popover/panel values (CONTEXT §code_context :225–311, §Executor-model guidance: sonnet for token swaps / CSS to a checklist)
requirements: [UX-13]
must_haves:
  truths:
    - "The shared controls panel stays dark (part of the menu-column surface) with the quiet-control treatment; it does not become a light card"
    - "The rename input keeps core's LIGHT input styling — core border/focus tokens, white field, dark text"
    - "The icon-picker and visibility popovers stay white cards whose border, shadow, tab treatment, and focus rings match core popover/postbox tokens exactly"
    - "Focus rings across panel + popovers are the core blue (#2271b1) ring, consistent with the rest of wp-admin"
    - "Colour is reserved for errors/destructive only — no decorative colour introduced in the panel or popovers"
    - "No storage/REST/behavioral change: icon-pick, visibility-toggle, and rename behavior are untouched — token alignment only"
  artifacts:
    - path: "assets/maestro.css"
      provides: "Panel + popover token alignment: quiet dark panel, core-light rename input, core popover/postbox card tokens (border/shadow/tab/focus)"
      contains: "2271b1"
  key_links:
    - from: "assets/maestro.css .maestro-popover / .maestro-icon-* / .maestro-vis-*"
      to: "WP core popover/postbox tokens"
      via: "border/shadow/focus values aligned to core (#c3c4c7, #2271b1, core card shadow)"
      pattern: "maestro-popover|maestro-icon-cell|maestro-icon-tab"
    - from: "assets/maestro.css .maestro-rename-input"
      to: "WP core light input tokens"
      via: "core border + focus-ring on a white field"
      pattern: "maestro-rename-input"
---

<objective>
Align the shared controls panel and the two popovers (icon picker, visibility) to
core wp-admin tokens per UX-13. This is mostly a token-alignment pass, not a rebuild:
the popover/panel values are already near core (CONTEXT §code_context :225–311). The
panel stays dark with the quiet-control treatment from plan 01; the rename input
keeps core's light input styling; the popovers stay white cards whose border, shadow,
tab, and focus-ring tokens match core popover/postbox exactly.

Purpose: with the toolbar (plan 01) and the pinned zone (plan 02) converted, the
panel and popovers are the remaining "chrome" surfaces. Bringing their borders,
shadows, tabs, and focus rings onto core tokens makes the whole editor read as native
wp-admin rather than a bespoke skin. Colour stays reserved for errors/destructive.

Output: token-aligned panel + popover CSS in assets/maestro.css, colour-scheme
inheritance applied where feasible, no behavioral change.
</objective>

<execution_context>
@/Users/danknauss/.claude/get-shit-done/workflows/execute-plan.md
@/Users/danknauss/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-editor-ux-polish/23-CONTEXT.md

# Depends on plan 02 (shared file). Read its SUMMARY for the pinned-zone changes so
# panel edits below the toolbar don't collide with the relocated mode-zone styling.
@.planning/phases/23-editor-ux-polish/23-02-SUMMARY.md

# Sole surface for this plan (verify line numbers before editing; file drifts after 01/02)
@assets/maestro.css

<interfaces>
<!-- CSS regions to align (assets/maestro.css, post-01/02 — verify line numbers). -->

Popovers (icon picker + visibility) — CONTEXT §code_context: "already near core
values … alignment pass, not rebuild":
- .maestro-popover (~:222): white card, border #c3c4c7, radius 4px, shadow
  0 4px 16px rgba(0,0,0,.18). Align border/radius/shadow to core's postbox/popover
  tokens (core uses #c3c4c7 borders; confirm the shadow matches core's card shadow
  rather than a bespoke value — adjust only if it diverges).
- .maestro-icon-search (~:235) border #8c8f94 — align to core light-input border/focus.
- .maestro-icon-tabs / .maestro-icon-tab (~:251–269): tab treatment (bottom-border
  active tab, #2271b1 accent, #50575e/#1d2327 text) — this is core's tab idiom; align
  any off values.
- .maestro-icon-cell (~:286–312): #f6f7f7 cell / hover #e0e7ec / focus 2px #2271b1 /
  current border #2271b1 bg #dbe9f5 — align to core selected/focus tokens.
- .maestro-vis-popover / .maestro-vis-head / .maestro-vis-row (~:313–324): align text
  colour + row spacing to core.

Panel (stays DARK — part of the menu-column surface, quiet-control treatment):
- .maestro-panel (~:451) dark zone with #3c434a left divider — keep dark; ensure the
  divider + label text use the menu-surface tokens (colour-scheme inheritance where
  feasible: currentColor/transparent so Modern/Midnight tint it; CONTEXT §WP-native).
- .maestro-panel-label / .maestro-panel-field (~:462–478) muted #c3c4c7 text — keep.
- .maestro-rename-input (~:479–498): KEEP core LIGHT input — white bg, #1d2327 text,
  core border (#c3c4c7 / #8c8f94), core focus ring (#2271b1). The square-corner /
  36px-height match to the buttons stays. Placeholder #50575e opacity:1 stays
  (WCAG 1.4.3, established in Phase 9 — do not regress).

Focus-ring consistency: every interactive control in panel + popovers uses the core
blue ring `outline: 2px solid #2271b1` (matches plan 01's quiet-control ring and the
selected-item ring at :201). Align any control still using a bespoke focus style.

Constraint: this plan touches ONLY assets/maestro.css. No JS, no i18n, no DOM. All
icon-pick / visibility / rename BEHAVIOR is frozen — token alignment only.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Align popover (icon + visibility) tokens to core popover/postbox</name>
  <files>assets/maestro.css</files>
  <action>
    Work the popover regions (CONTEXT §code_context :222–324; verify current lines):
    - .maestro-popover: confirm border #c3c4c7, radius 4px, and the card shadow match
      core's popover/postbox card tokens. If the shadow (0 4px 16px rgba(0,0,0,.18))
      diverges from core's standard card shadow, align it; otherwise leave. Stay a
      white card (do NOT darken — popovers are light per CONTEXT §WP-native styling).
    - .maestro-icon-search: align the input border + focus to core light-input tokens
      (#8c8f94 border, #2271b1 focus ring) so the search field matches the rename input
      idiom. Add/confirm a focus ring if missing.
    - .maestro-icon-tabs / .maestro-icon-tab / [aria-selected]: this is core's tab
      idiom (transparent tab, active tab bottom-border #2271b1, weight 600). Align text
      colours to core (#50575e inactive, #1d2327 active) and confirm the accent is
      #2271b1. No decorative colour.
    - .maestro-icon-cell + :hover + :focus-visible + .is-current: align to core
      selected/hover/focus tokens (focus 2px #2271b1; current border #2271b1). Keep the
      44px touch target in the <=782px block (do not regress WCAG 2.5.5).
    - .maestro-vis-popover / .maestro-vis-head / .maestro-vis-row: align text colour +
      spacing to core; keep the touch-target row padding at <=782px.
    Reserve colour for errors/destructive only — introduce NO new decorative colour.
    Behavior frozen: do not touch any JS or DOM; class names stay (e2e targets them —
    any assertion change lands in plan 23-05, not here).
  </action>
  <verify>
    <automated>grep -nE "maestro-popover|maestro-icon-tab|maestro-icon-cell|maestro-vis-" assets/maestro.css | head</automated>
    <automated>! grep -nE "#6bc187|#f0b849|#dba617" assets/maestro.css</automated>
  </verify>
  <done>Popovers stay white cards on core popover/postbox tokens (border #c3c4c7, core card shadow, radius 4px); tabs use core's #2271b1 active-tab idiom; cells + search use core hover/focus/selected tokens with the #2271b1 ring; touch targets preserved at <=782px; no decorative or removed-legacy colour present.</done>
</task>

<task type="auto">
  <name>Task 2: Keep the panel dark/quiet; keep the rename input core-light; unify focus rings</name>
  <files>assets/maestro.css</files>
  <action>
    - .maestro-panel + divider: keep the panel DARK (part of the menu-column surface,
      quiet-control treatment from plan 01). Confirm the left divider (#3c434a) and the
      label/field text (#c3c4c7) use menu-surface tokens; where feasible switch the
      divider/label to transparent/currentColor so Modern/Midnight schemes tint them
      (CONTEXT §WP-native colour-scheme inheritance). Hardcode only where WP exposes no
      scheme variable — note any hardcode in the SUMMARY.
    - .maestro-rename-input: KEEP core light-input styling — white bg, #1d2327 text,
      core border, #2271b1 focus ring. Add/confirm the focus ring uses the core blue
      ring consistent with the popovers + quiet controls. Do NOT regress the
      placeholder #50575e opacity:1 rule (WCAG 1.4.3, Phase 9) or the 36px height /
      square-corner button match.
    - Focus-ring consistency pass: ensure every interactive panel/popover control uses
      `outline: 2px solid #2271b1` (matching plan 01's quiet-control ring and the
      selected-item ring at ~:201). Replace any lingering bespoke focus style.
    No JS/DOM/i18n changes; rename/visibility/icon behavior frozen. Colour reserved for
    errors/destructive only.
  </action>
  <verify>
    <automated>grep -nE "maestro-rename-input|maestro-panel" assets/maestro.css | head</automated>
    <automated>grep -c "2271b1" assets/maestro.css</automated>
  </verify>
  <done>Panel stays dark/quiet with menu-surface (ideally inheritable) tokens; rename input keeps core-light styling with the #2271b1 focus ring and the preserved placeholder-contrast rule; all panel/popover focus rings are the consistent core-blue ring; no behavioral change.</done>
</task>

</tasks>

<verification>
Maps to ROADMAP success criterion 3 (shared panel + icon/visibility popovers adopt
core idioms, spot-checked on Default + Modern + Midnight — confirmed by per-surface
before/after screenshots in plan 23-05) and criterion 5 (suites stay green; any e2e
token/colour assertion on these surfaces is updated deliberately in plan 23-05).

TDD boundary (CONTEXT §Methodology): pure styling — NO unit TDD (no logic changes in
this plan). Covered by Playwright + before/after screenshots in plan 23-05 on the three
colour schemes. No invented unit tests.

- `grep` confirms the removed-legacy colours (#6bc187/#f0b849/#dba617) are absent and
  the core blue (#2271b1) ring is present across panel + popovers.
- Behavior frozen: no JS/DOM/i18n touched; class names unchanged (e2e-safe).
- Colour-scheme inheritance / any hardcode noted in the SUMMARY.
</verification>

<success_criteria>
- Shared panel stays dark + quiet (menu-surface tokens, inheritable where feasible).
- Rename input keeps core light-input styling with the core-blue focus ring; placeholder-contrast preserved.
- Icon + visibility popovers stay white cards on core popover/postbox tokens (border/shadow/tab/focus).
- Focus rings across panel + popovers are the consistent core blue (#2271b1).
- No decorative colour; colour reserved for errors/destructive; no behavioral change.
</success_criteria>

<output>
After completion, create `.planning/phases/23-editor-ux-polish/23-03-SUMMARY.md`.
Record: which popover values actually diverged from core and were aligned (vs already
matching); any admin-colour-scheme inheritance applied to the panel and any token that
had to stay hardcoded because WP exposes no scheme variable.
</output>
