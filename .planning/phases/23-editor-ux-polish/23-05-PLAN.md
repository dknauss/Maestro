---
phase: 23-editor-ux-polish
plan: 05
type: execute
wave: 5
depends_on: [01, 02, 03, 04]
files_modified:
  - tests/e2e/editor.spec.ts
  - tests/e2e/tour.spec.ts
  - tests/e2e/specs/capture-screenshots.spec.ts
  - docs/current-metrics.md
autonomous: false
executor_model: sonnet   # mechanical e2e selector/colour-assertion updates + MAESTRO_CAPTURE screenshot capture (CONTEXT §Executor-model guidance: sonnet for e2e selector updates + screenshot capture)
requirements: [UX-13]
must_haves:
  truths:
    - "Every e2e assertion that referenced a removed class or colour (semantic-colour borders, green mode chip #6bc187, amber #f0b849, old toolbar-position mode/status zone) is updated DELIBERATELY to the new native idiom — never silently deleted"
    - "Before/after screenshots are captured per surface (toolbar, pinned zone, panel, popovers, coachmark, first-run centering, in-menu selection/badge), MAESTRO_CAPTURE-gated so normal e2e never churns committed PNGs"
    - "Screenshots are spot-checked on Default + Modern + Midnight admin colour schemes"
    - "Accessibility check passes: every edit-mode state carries a non-colour signal (icon/text/SR-text) — WCAG 1.4.1"
    - "All suites are green at current baselines: PHP unit + integration, npm run test:js, Playwright e2e; Plugin Check 0 errors; WPCS + PHPStan clean"
    - "docs/current-metrics.md is updated in the same commit as any change to a counted quantity (e2e test count, assertion count)"
  artifacts:
    - path: "tests/e2e/editor.spec.ts"
      provides: "Updated selector/colour assertions for the restyled toolbar + relocated mode/status zone + dot modified badge + selection"
      contains: "maestro-mode"
    - path: "tests/e2e/tour.spec.ts"
      provides: "Updated coachmark assertions for the wp-pointer look (behavior assertions unchanged; only class/token assertions that drifted)"
      contains: "maestro-tour"
    - path: "tests/e2e/specs/capture-screenshots.spec.ts"
      provides: "MAESTRO_CAPTURE-gated before/after captures per surface on Default + Modern + Midnight"
      contains: "MAESTRO_CAPTURE"
    - path: "docs/current-metrics.md"
      provides: "Re-derived counts for any changed counted quantity (e2e tests/assertions)"
      contains: "current-metrics"
  key_links:
    - from: "tests/e2e/editor.spec.ts + tour.spec.ts"
      to: "the restyled surfaces from plans 01-04"
      via: "selector/colour assertions matched to the new native idioms"
      pattern: "maestro-mode|maestro-status|maestro-modified|maestro-tour"
    - from: "tests/e2e/specs/capture-screenshots.spec.ts"
      to: "committed before/after PNGs per surface"
      via: "MAESTRO_CAPTURE gate + per-scheme capture"
      pattern: "MAESTRO_CAPTURE"
---

<objective>
Reconcile the tests to the restyle and prove the phase's zero-regression bar. Update
every Playwright/e2e selector + colour assertion that references a removed class or
colour — DELIBERATELY, never silently (this is where ALL the in-plan e2e updates the
earlier plans promised actually land). Capture before/after screenshots per surface
(MAESTRO_CAPTURE-gated), spot-checked on Default + Modern + Midnight. Run the
accessibility check (every state carries a non-colour signal, WCAG 1.4.1). Confirm all
suites green + Plugin Check 0 errors + WPCS/PHPStan clean, updating docs/current-metrics.md
on sight if a counted quantity changed.

Purpose: plans 01-04 changed classes, colours, and DOM positions that e2e assertions
target. Per CONTEXT §UAT/verification, those assertions are updated deliberately here,
in one place, with the reasoning visible — not silently mutated inside each restyle
plan. This is the final wave: it depends on all four prior plans.

Output: reconciled e2e specs, per-surface before/after screenshots on three schemes, a
recorded accessibility pass, a green full-suite gate, and an updated metrics doc.
</objective>

<execution_context>
@/Users/danknauss/.claude/get-shit-done/workflows/execute-plan.md
@/Users/danknauss/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-editor-ux-polish/23-CONTEXT.md

# This plan reconciles tests to ALL prior plans — read every SUMMARY for the exact
# class/colour/DOM changes each made (dot glyph, relocation target, wp-pointer decision).
@.planning/phases/23-editor-ux-polish/23-01-SUMMARY.md
@.planning/phases/23-editor-ux-polish/23-02-SUMMARY.md
@.planning/phases/23-editor-ux-polish/23-03-SUMMARY.md
@.planning/phases/23-editor-ux-polish/23-04-SUMMARY.md

# Test + doc surfaces (verify line numbers before editing)
@tests/e2e/editor.spec.ts
@tests/e2e/tour.spec.ts
@tests/e2e/specs/capture-screenshots.spec.ts
@docs/current-metrics.md

<interfaces>
<!-- e2e assertions that WILL drift (grounded in current specs — verify lines). -->

editor.spec.ts assertions touching restyled/moved surfaces:
- ~L546-549: .maestro-mode-label is the persistent indicator; .maestro-status is the
  transient save-status. After plan 02 the mode/status zone RELOCATES into the menu
  column > 782px — any assertion that assumes it lives under `.maestro-toolbar`
  (e.g. `page.locator('.maestro-toolbar .maestro-mode-label')` at ~L549, ~L651, ~L670,
  ~L678, ~L684, ~L617) must be updated to find it wherever plan 02 docks it (menu column
  at desktop width; toolbar at <=782px). Update DELIBERATELY per 23-02-SUMMARY's
  relocation target — do not just loosen the selector blindly.
- ~L647: "persistent .maestro-mode-label is icon-only with pencil dashicon and 'Edit
  Mode' accessible name" — mode chip is now NEUTRAL (no green). Update any colour
  assertion (#6bc187) to the neutral token from 23-01-SUMMARY.
- ~L632-636: status class toggling saved/idle — recolour assertions to plan 01's muted
  states (grey Saved / red error). Update any #6bc187/#f0b849 colour expectations.
- ~L310-347, ~L933-961: .maestro-modified / .maestro-modified-badge / .maestro-modified-sr
  — badge switched to the plan-01 dot idiom (23-04). Keep the class hooks; update any
  glyph/colour/shape assertion to the dot.

tour.spec.ts:
- Behavior assertions (5 steps, focus trap, Skip/Back/Next, replay) are FROZEN — they
  must stay green UNCHANGED (that is the proof the restyle didn't touch behavior). Update
  ONLY class/token assertions that drifted from the wp-pointer restyle (per 23-04-SUMMARY:
  if the wp-pointer look was enqueued/replicated with new classes, reconcile selectors).

Removed-colour sweep — assert absence across the suite where meaningful:
- #6bc187 (green), #f0b849 (amber), #dba617, the old semantic-border classes. Any e2e
  computed-style assertion on these must move to the new tokens.

Screenshot capture (tests/e2e/specs/capture-screenshots.spec.ts):
- Already MAESTRO_CAPTURE-gated (CONTEXT §code_context; STATE resolved the churn — specs
  write to tests/e2e/screenshots/…). Add before/after captures per surface: toolbar,
  pinned mode-zone (menu column), panel, icon popover, visibility popover, coachmark,
  first-run centering, in-menu selection + modified dot. Capture on Default + Modern +
  Midnight admin colour schemes (set the user's admin_color / color scheme per capture
  set — mirror how the existing capture specs set state). Gate stays MAESTRO_CAPTURE so a
  normal `npm run test:e2e` never regenerates committed PNGs.

Full-suite gate + metrics:
- `npm run test:js` (node --test tests/js/*.mjs), `npm run test:e2e` (Playwright), PHP
  unit + integration, Plugin Check (--exclude-directories per STATE 17-03 pattern:
  tests,bin,docs,build,vendor,node_modules,playground,.planning,.claude,.github), WPCS
  (phpcs) + PHPStan. e2e may need Docker/wp-env (sandbox-disabled) on an alternate port
  if 8889 is taken (STATE: WP_ENV_TESTS_PORT=8899 pattern) — the orchestrator runs the
  Docker-dependent gate at the wave boundary if per-task e2e is blocked (STATE Phase 09
  wave-boundary e2e gate pattern).
- docs/current-metrics.md: each counted quantity carries a re-derivation command. If the
  e2e test/assertion count changed, UPDATE it in the SAME commit (project CLAUDE.md
  canonical-metrics rule); run the repo's metrics check before pushing.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reconcile e2e selector + colour assertions to the restyle (deliberately)</name>
  <files>tests/e2e/editor.spec.ts, tests/e2e/tour.spec.ts</files>
  <action>
    Go through every e2e assertion that references a removed class, a removed colour, or
    the old mode/status TOOLBAR position, and update it DELIBERATELY to the new idiom —
    documenting the reason inline where non-obvious (never silently delete an assertion).
    Use each prior SUMMARY as the source of truth for the new class/colour/position.
    - editor.spec.ts: update the .maestro-mode-label / .maestro-status locators that
      assume `.maestro-toolbar` ancestry to find the zone at its plan-02 location (menu
      column > 782px; toolbar <=782px) — assert BOTH placements where the spec already
      exercises widths. Recolour the neutral mode chip + muted save-status assertions
      (drop #6bc187/#f0b849 expectations; assert the plan-01 muted tokens). Update the
      .maestro-modified-badge assertions to the dot idiom (keep .maestro-modified /
      .maestro-modified-badge / .maestro-modified-sr hooks). Keep the idle
      .maestro-status::before content:none assertion (still valid — icon is a DOM child).
    - tour.spec.ts: leave the BEHAVIOR assertions (steps/trap/Skip/Back/Next/replay)
      untouched — they must stay green as-is to prove behavior didn't change. Update ONLY
      class/token assertions that drifted from the wp-pointer restyle (per 23-04-SUMMARY).
    Run `npm run test:js` (unaffected but a fast sanity gate) and, where the environment
    allows, the affected Playwright specs; otherwise flag for the wave-boundary Docker
    gate in Task 3.
  </action>
  <verify>
    <automated>npx playwright test tests/e2e/editor.spec.ts tests/e2e/tour.spec.ts --list</automated>
    <automated>grep -nE "6bc187|f0b849" tests/e2e/editor.spec.ts tests/e2e/tour.spec.ts || echo "no stale colours in specs"</automated>
  </verify>
  <done>Every drifted selector/colour assertion is updated to the new native idiom with the reasoning visible; behavior assertions in tour.spec.ts are untouched; no removed-colour (#6bc187/#f0b849) expectation remains in the specs; the specs list/parse cleanly.</done>
</task>

<task type="auto">
  <name>Task 2: Capture before/after screenshots per surface on Default + Modern + Midnight</name>
  <files>tests/e2e/specs/capture-screenshots.spec.ts</files>
  <action>
    Extend the MAESTRO_CAPTURE-gated capture spec to produce before/after screenshots for
    each converted surface: bottom toolbar, pinned mode-zone (menu column, > 782px), the
    <=782px rejoined toolbar, shared panel, icon popover, visibility popover, coachmark
    (first step — proves BUG-08 centering), in-menu selection + modified dot. Capture each
    surface on the three admin colour schemes in the UAT set: Default + Modern + Midnight
    (set the admin colour scheme per capture set the way the existing capture specs set
    editor state; Light is deliberately NOT in the set — CONTEXT §UAT/verification). Keep
    the MAESTRO_CAPTURE gate so `npm run test:e2e` never regenerates committed PNGs; write
    to tests/e2e/screenshots/… (STATE: capture-spec output path resolved 2026-07-04).
    Run `npm run screenshots` (or the MAESTRO_CAPTURE-gated invocation) where the
    environment allows; otherwise flag for the wave-boundary Docker gate in Task 3.
  </action>
  <verify>
    <automated>grep -n "MAESTRO_CAPTURE" tests/e2e/specs/capture-screenshots.spec.ts</automated>
    <automated>npx playwright test tests/e2e/specs/capture-screenshots.spec.ts --list</automated>
  </verify>
  <done>The capture spec produces before/after PNGs for every converted surface on Default + Modern + Midnight, MAESTRO_CAPTURE-gated (normal e2e does not churn PNGs), written to tests/e2e/screenshots/…; the spec lists/parses cleanly.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Full-suite green gate + accessibility check + metrics (final phase gate)</name>
  <what-built>
    The complete Phase 23 restyle (plans 01-04) plus the reconciled e2e specs (Task 1)
    and per-surface/per-scheme screenshots (Task 2). This checkpoint is the phase's
    zero-regression gate: it runs the Docker-dependent suites (which the sandbox blocks
    per-task) and confirms the accessibility bar + metrics doc.
  </what-built>
  <files>tests/e2e/editor.spec.ts, tests/e2e/tour.spec.ts, tests/e2e/specs/capture-screenshots.spec.ts, docs/current-metrics.md</files>
  <action>
    Run the Docker-dependent full suite (sandbox-disabled wp-env, alternate port via
    WP_ENV_TESTS_PORT if 8889 is held): npm run test:js, npm run test:e2e, PHP unit +
    integration, Plugin Check (0 errors), phpcs (WPCS) + PHPStan. Review the per-surface
    before/after screenshots on Default + Modern + Midnight, run the WCAG 1.4.1
    non-colour-signal accessibility check, and reconcile docs/current-metrics.md in the
    same commit if any counted quantity changed. Present the gate results for sign-off
    and record them in the SUMMARY.
  </action>
  <how-to-verify>
    Bring up wp-env (sandbox-disabled; if port 8889 is held, start on the alternate ports
    and run with WP_ENV_TESTS_PORT=8899 per STATE Phase 11.1/17-03). Then:
    1. Full-suite gate — ALL green at current baselines:
       - `npm run test:js` (node:test)
       - `npm run test:e2e` (Playwright: editor.spec.ts + tour.spec.ts + the rest)
       - PHP unit + integration (composer test / phpunit) — including the localized
         payload-budget integration test (no i18n budget regression)
       - Plugin Check: 0 errors on shippable source (--exclude-directories:
         tests,bin,docs,build,vendor,node_modules,playground,.planning,.claude,.github)
       - WPCS (phpcs) clean + PHPStan 0 errors
    2. Screenshots: review the before/after captures per surface on Default + Modern +
       Midnight — confirm each surface reads as native wp-admin and nothing regressed.
    3. Accessibility check (WCAG 1.4.1): confirm EVERY edit-mode state carries a
       non-colour signal — mode chip (pencil + text), save status (spinner/check/warning
       + SR text), modified (dot + SR "(modified)"), selection (accent bar + focus ring),
       Reset All (text label). No state distinguished by colour alone.
    4. Metrics: if the e2e test/assertion count (or any counted quantity in
       docs/current-metrics.md) changed, update it in the same commit and run the repo's
       metrics check (`bash .github/scripts/verify-metrics.sh docs/current-metrics.md` or
       `composer verify:metrics`).
    Record the gate results (suite counts) + the accessibility pass + any metrics delta in
    the SUMMARY.
  </how-to-verify>
  <verify>
    <automated>npm run test:js</automated>
    <manual>Full-suite gate (Playwright + PHP unit/integration + Plugin Check + WPCS/PHPStan) green on wp-env; accessibility + screenshot review (steps below).</manual>
  </verify>
  <done>All suites are green at current baselines (PHP unit/integration, npm run test:js, Playwright); Plugin Check 0 errors; WPCS + PHPStan clean; the accessibility check passes (every state carries a non-colour signal); before/after screenshots reviewed on all three schemes; docs/current-metrics.md reconciled with any counted-quantity change.</done>
  <resume-signal>Type "approved" once every suite is green, Plugin Check is 0 errors, WPCS/PHPStan are clean, the accessibility check passes, and metrics are reconciled — or report the failures.</resume-signal>
</task>

</tasks>

<verification>
Maps to ROADMAP success criterion 5 (all suites stay green at current baselines with e2e
assertions updated deliberately in-plan; Plugin Check 0 errors) and is the verification
umbrella confirming criteria 1-4 via the per-surface before/after screenshots on Default
+ Modern + Midnight (mode-zone pinning, semantic-colour removal + accessibility, all-surface
core idioms, first-run centering).

TDD boundary (CONTEXT §Methodology): no changed pure logic in this plan — the two
pure-logic seams (modeStatusLabel in plan 01, modeZonePlacement in plan 02) are already
covered by node:test. This plan's proof is Playwright + before/after screenshots +
the full-suite gate. No invented unit tests.

- e2e specs reconciled deliberately (Task 1); no stale removed-colour expectations remain.
- MAESTRO_CAPTURE-gated captures per surface on three schemes (Task 2); no PNG churn on
  normal e2e.
- Full-suite green + Plugin Check 0 + WPCS/PHPStan clean + accessibility pass + metrics
  reconciled (Task 3, wave-boundary Docker gate).
</verification>

<success_criteria>
- Every drifted e2e selector/colour assertion updated deliberately to the new idiom; tour behavior assertions untouched and green.
- Before/after screenshots per surface captured on Default + Modern + Midnight, MAESTRO_CAPTURE-gated, no PNG churn on normal e2e.
- Accessibility check passes: every edit-mode state carries a non-colour signal (WCAG 1.4.1).
- Full suite green (PHP unit + integration, npm run test:js, Playwright); Plugin Check 0 errors; WPCS + PHPStan clean.
- docs/current-metrics.md reconciled in the same commit as any counted-quantity change.
</success_criteria>

<output>
After completion, create `.planning/phases/23-editor-ux-polish/23-05-SUMMARY.md`.
Record: the exact e2e assertions changed and why (per surface); the screenshot set
captured (surfaces x schemes); the full-suite gate results (suite counts / Plugin
Check / WPCS / PHPStan) and the port/Docker context; the accessibility-check outcome;
and any docs/current-metrics.md delta with its re-derivation command.
</output>
