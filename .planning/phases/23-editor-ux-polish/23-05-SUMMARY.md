---
phase: 23-editor-ux-polish
plan: 05
subsystem: testing
tags: [playwright, e2e, node-test, accessibility, wcag, screenshots, wp-env]

# Dependency graph
requires:
  - phase: 23-editor-ux-polish (01-04)
    provides: the restyled bottom toolbar, Exit-consolidation onto the admin-bar toggle, shared-panel/popover core tokens, and wp-pointer coachmark that this plan's e2e assertions and screenshots verify
provides:
  - Reconciled Playwright specs (editor.spec.ts, tour.spec.ts) asserting the new native idioms instead of the removed mode-chip/bottom-Exit/semantic-colour DOM
  - A save-flush-on-exit e2e proving the admin-bar toggle's click intercept awaits the pending autosave before navigating
  - 21 committed before/after PNGs (7 surfaces x 3 admin colour schemes: Default/Modern/Midnight) at tests/e2e/screenshots/surfaces/
  - A recorded, live-run full-suite gate (JS/e2e/PHP unit/PHP integration/WPCS/PHPStan) and a WCAG 1.4.1 accessibility pass, closing out Phase 23
affects: [Phase 24 (release build-then-check should re-run Plugin Check against the built zip, not the dev-tree checkout)]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "e2e reconciliation is a dedicated final-wave plan, not silent edits inside each restyle plan — the reasoning for every changed selector/colour assertion lives in one commit"
    - "MAESTRO_CAPTURE-gated screenshot spec extended per-surface x per-scheme, never regenerated by normal `npm run test:e2e`"
    - "wave-boundary Docker gate: full suite run once at the plan-05 checkpoint rather than per-task, per the established Phase 09/11 pattern"

key-files:
  created:
    - tests/e2e/screenshots/surfaces/ (21 PNGs: 7 surfaces x Default/Modern/Midnight)
  modified:
    - tests/e2e/editor.spec.ts
    - tests/e2e/specs/capture-screenshots.spec.ts

key-decisions:
  - "Plugin Check's 4 errors + 6 warnings are pre-existing dev-tree root files (hidden dotfiles, dev tooling configs, non-standard root markdown, readme.txt upgrade-notice length) untouched by any Phase 23 commit — logged to deferred-items.md as a Phase 24 (REL-10) build-then-check follow-up, not fixed here (out of this plan's scope, matches prior 17-03 gate's identical invocation)"
  - "tour.spec.ts required no changes — its classes/tokens already matched 23-04's wp-pointer restyle; only editor.spec.ts needed reconciliation"
  - "Reset All cleanup click in the new save-flush-on-exit test hardened with force:true after a reproducible Playwright actionability-wait hang (element verified stable/clickable by every manual check; same escape hatch already used for this suite's position:fixed toolbar click-delivery quirks per STATE's 11-08 note)"

patterns-established:
  - "Deliberate e2e reconciliation as its own wave-boundary plan when multiple prior plans changed DOM/colour that assertions target — one commit documents every drift with the reasoning, never a silent delete"

requirements-completed: [UX-13]

# Metrics
duration: ~45min
completed: 2026-07-05
---

# Phase 23 Plan 05: E2E Reconciliation, Cross-Scheme Screenshots & Full-Suite Gate Summary

**Reconciled Playwright specs to the Exit-consolidation restyle, captured 21 before/after PNGs across Default/Modern/Midnight, and closed Phase 23 with a live full-suite gate (JS 53/53, e2e 31/31, PHP unit 90/90, PHP integration 47/47, WPCS/PHPStan clean, WCAG 1.4.1 accessibility confirmed) — Plugin Check's 4 pre-existing dev-tree findings deferred to Phase 24's build-then-check pipeline.**

## Performance

- **Duration:** ~45 min
- **Tasks:** 3 (2 auto + 1 checkpoint gate)
- **Files modified:** 2 spec files + 21 new PNGs

## Accomplishments

- Deleted every e2e assertion referencing the removed `.maestro-mode-label` pencil-chip and the removed bottom-toolbar `.maestro-exit` (plan 02's Exit-consolidation), replacing them with an assertion against the WP Toolbar admin-bar toggle (`#wp-admin-bar-maestro-toggle`, "Exit Menu Editor" label) — the single entry/exit
- Added a new save-flush-on-exit behaviour test: edit an item, click the Toolbar exit, confirm the change persisted after reload, proving the plan-02 click-intercept correctly awaits the pending autosave
- Confirmed `.maestro-status` stays under `.maestro-toolbar` (no relocation) and recoloured save-status/modified-badge assertions off the retired `#6bc187`/`#f0b849` semantic tokens
- Extended the MAESTRO_CAPTURE-gated capture spec with 18 new tests producing 21 before/after PNGs across 7 converted surfaces (bottom toolbar + Reset All, admin-bar "Exit Menu Editor" toggle, shared panel, icon popover, visibility popover, coachmark, in-menu selection + modified dot) on Default, Modern, and Midnight admin colour schemes — Modern/Midnight's first spot-check for this phase (23-04 verified Default only)
- Ran the phase's zero-regression gate live against wp-env (sandbox-disabled): all suites green, WCAG 1.4.1 accessibility check passed, and the one non-green finding (Plugin Check dev-tree noise) was triaged as out-of-scope pre-existing and logged rather than silently absorbed

## Task Commits

1. **Task 1: Reconcile e2e selector + colour assertions to the restyle** — `fdb647a` (test) + `df7e207` (fix — hardened a Playwright actionability hang on the new test's cleanup click)
2. **Task 2: Capture before/after screenshots per surface on Default/Modern/Midnight** — `71b0f8e` (test)
3. **Task 3: Full-suite green gate + accessibility check** — checkpoint (human-verify); no code changes; results recorded below and approved by the orchestrator on the user's behalf ("proceed through 23 without stopping")

**Plan progress/checkpoint record:** `76d42e7` (docs: record Tasks 1-2 progress + Task 3 gate results; paused at checkpoint)

## Files Created/Modified

- `tests/e2e/editor.spec.ts` — mode/exit assertions reconciled to the admin-bar toggle; save-flush-on-exit test added; cleanup click hardened
- `tests/e2e/specs/capture-screenshots.spec.ts` — 18 new MAESTRO_CAPTURE-gated capture tests, 7 surfaces x 3 schemes
- `tests/e2e/screenshots/surfaces/` — 21 committed PNGs (before/after per surface, Default/Modern/Midnight)
- `tests/e2e/tour.spec.ts` — verified, no changes needed (already matched 23-04's classes)

## Decisions Made

- Plugin Check's findings are pre-existing dev-tree root files, verified via `git diff main...HEAD` to be untouched by any Phase 23 commit; matches the identical `--exclude-directories` invocation that reported "0 errors" in the 17-03 gate against this same file set, so most likely ruleset/version drift (wp-env's plugin-check 2.0.0) or a historical run against a built ZIP rather than the raw dev-tree checkout. Deferred to Phase 24 (REL-10) to re-run against the actual release ZIP before tagging v1.4.0 — see `deferred-items.md`.
- tour.spec.ts needed zero changes — its behavior assertions were always frozen, and its class/token assertions already matched 23-04's wp-pointer restyle, so there was nothing to reconcile.
- The save-flush-on-exit test's Reset All cleanup click required `force:true` after a reproducible Playwright actionability-wait hang despite every manual stability check (bounding box, hit-test, enabled/opacity/pointer-events) passing — consistent with this suite's known `position:fixed` toolbar click-delivery quirk (STATE 11-08 race(b)), not a product defect.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] Hardened save-flush-on-exit cleanup click against a Playwright actionability hang**
- **Found during:** Task 1 (live verification of the new save-flush-on-exit test against wp-env)
- **Issue:** The test's cleanup click on Reset All hung on Playwright's built-in "wait until stable" click precondition, despite the element's bounding box, hit-test, enabled state, opacity, and pointer-events all being verified stable — a known quirk of this suite's `position:fixed` toolbar (same root cause as the 11-08 race(b) note in STATE.md)
- **Fix:** Added `force: true` to bypass only the actionability wait, not a real obstruction — the same escape hatch this suite already relies on elsewhere for this exact class of click-delivery issue
- **Files modified:** tests/e2e/editor.spec.ts
- **Verification:** Test passes reliably in the Task 3 full-suite gate run
- **Committed in:** df7e207

---

**Total deviations:** 1 auto-fixed (1 blocking)
**Impact on plan:** Necessary to get a genuinely new e2e test past a pre-existing suite-level click-delivery quirk; no scope creep, no product code touched.

### Out-of-Scope Discoveries (logged, not fixed)

Plugin Check flagged 4 errors + 6 warnings against pre-existing dev-tree root files (hidden dotfiles, dev tooling configs, non-standard root markdown, readme.txt upgrade-notice length) — none created or modified by any Phase 23 plan. Logged to `deferred-items.md` per the executor's scope-boundary rule, with a Phase 24 (REL-10) build-then-check recommendation. See that file for the full file list and reasoning.

## Issues Encountered

None beyond the actionability-hang fix documented above.

## Gate Results (Task 3 — recorded, not re-run)

Run live against wp-env (sandbox-disabled), `WP_ENV_TESTS_PORT=8899`:

| Suite | Result |
|---|---|
| `npm run test:js` (node:test) | 53/53 pass |
| Playwright e2e (full suite) | 31 passed, 28 skipped (MAESTRO_CAPTURE-gated, correctly not run), 0 failed |
| PHP unit | 90/90 pass |
| PHP integration (wp-env tests-cli) | 47/47 pass |
| WPCS (`composer lint`) | clean, 0 issues |
| PHPStan (`composer analyse:phpstan`) | 0 errors |
| Plugin Check | 4 errors + 6 warnings — **all pre-existing dev-tree root files, none touched by Phase 23**; 0 errors on Phase 23's shipped code (CSS/JS/PHP). See "Out-of-Scope Discoveries" above and `deferred-items.md`. |
| Accessibility (WCAG 1.4.1) | PASS — every edit-mode state carries a non-colour signal: edit mode = admin-bar toggle dashicon + "Exit Menu Editor" text; save status = per-state glyphs + `wp.a11y.speak()`; modified = dot shape + SR text; selection = accent bar + focus ring; Reset All = icon + text label |
| Screenshots | 7 surfaces x 3 schemes (Default/Modern/Midnight) = 21 PNGs committed at `tests/e2e/screenshots/surfaces/` — Modern/Midnight's first spot-check this phase (23-04 verified Default only) |

**Honest Plugin Check framing:** this is not a flat "0 errors" result. It is 0 errors on all code Phase 23 shipped (CSS/JS/PHP), with 4 pre-existing dev-tree findings against files no Phase 23 plan touched, deferred to Phase 24's build-then-check pipeline against the actual release ZIP.

**Sign-off:** the orchestrator reviewed and approved this gate on the user's behalf ("proceed through 23 without stopping"). No further suite runs were performed for this closing plan — the results above are the authoritative record.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

Phase 23 (Editor UX Polish) is complete: all 5 plans done, UX-09/UX-12/UX-13/BUG-08 all delivered. Next per the 2026-07-04 ordering decision: Phase 19 planning resumes (ROLE-01 feasibility note), then Phase 24 (Release v1.4.0) should re-run Plugin Check against the built release ZIP per this plan's deferred-items.md recommendation before tagging v1.4.0.

---
*Phase: 23-editor-ux-polish*
*Completed: 2026-07-05*
